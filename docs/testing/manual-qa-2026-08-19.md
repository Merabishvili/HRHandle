# Manual QA — Full Website Walkthrough (2026-08-19)

_Running log of a full end-to-end manual test of the live flows. Each entry records
what was tested, the result, and any issue found with a fix-timing flag._

**Fix-timing legend:**
- 🔴 **fix-during** — blocking-ish; fix while testing continues
- 🟡 **fix-after** — real issue but batch the fix after the walkthrough is complete
- 🟢 **pass** — works as expected, no action

---

## 1. Landing page

**Result:** 🟢 pass, with 🟡 follow-ups.

- Landing-page translation renders correctly for the selected language.
- All buttons clicked → behave correctly.
- Register / Login pages open translated in the selected language.

**🟡 fix-after — follow-ups:**
- Legal pages not yet translated: **Terms & Conditions**, **Refund Policy**, **Privacy Policy** (still English-only).
- **Guide** not translated **and** the guide content is an **old version** — needs a content refresh + translation.

---

## 2. Registration (email/password, no Google/MS)

**Result:** 🔴 fix-during — one bug.

- Registered directly (no OAuth) with **Georgian** UI language selected.
- **BUG:** Sign-up confirmation email arrived in **English**, should have been **Georgian**
  (match the UI language chosen at sign-up).
- Confirmed via the English email link → pipeline opened correctly in Georgian.

**Root cause:** the confirmation email is sent by **Supabase Auth** using a single fixed
template configured in the Supabase dashboard; the app never passes the chosen locale, and
the template has no per-language branching. See fix plan → *Fix A*.

---

## 3. Authorization / Login

**Result:** 🔴 fix-during — one bug (public vacancy URL).

- Logged in with "remember me" checked → session persisted, dashboard opened. 🟢
- **BUG:** The **public vacancy URL** (org `public_page_slug`) is **not filled** — it renders as
  just a single `-` character.

**Root cause:** `slugify()` in [`lib/onboarding.ts`](../../lib/onboarding.ts) does
`.replace(/[^a-z0-9\s-]/g, '')`, which strips **all** non-Latin characters. A Georgian/Russian
company name (e.g. `კომპანია`) collapses to `''` or, if it had a space, to a lone `-`. The slug
should be **transliterated** Georgian/Russian → Latin before slugifying, so the org name still
produces a usable public URL. See fix plan → *Fix B*.

---

## Fix plan (summary)

| ID | Issue | Timing | Type |
|----|-------|--------|------|
| A | Sign-up confirmation email not localized | 🔴 during | code (pass locale) + **Supabase dashboard template edit on BOTH projects** |
| B | Public vacancy URL empty for non-Latin org names | 🔴 during | code (transliteration) + tests |
| C | Legal pages (Terms/Refund/Privacy) untranslated | 🟡 after | i18n content |
| D | Guide outdated + untranslated | 🟡 after | content + i18n |

## Resolution status (2026-08-19)

**Fix B — public URL / transliteration → ✅ code done.**
- New [`lib/i18n/transliterate.ts`](../../lib/i18n/transliterate.ts) (Georgian 2002 + Cyrillic → Latin).
- [`lib/onboarding.ts`](../../lib/onboarding.ts) `slugify()` now transliterates first and strips
  leading/trailing hyphens, so a non-Latin name can never produce a lone `-`.
- Tests: [`lib/i18n/__tests__/transliterate.test.ts`](../../lib/i18n/__tests__/transliterate.test.ts)
  + Georgian/Russian/lone-`-` cases in [`lib/__tests__/onboarding.test.ts`](../../lib/__tests__/onboarding.test.ts).
- **Existing broken row:** staging `organizations` is **empty (0 rows)** — the `-` slug you saw
  is on **production**. A prod DB repair is **pending explicit production authorization** (not done).

**Fix A — localized sign-up email → ✅ code done; ⏳ dashboard step pending.**
- Code: [`components/auth/sign-up-form.tsx`](../../components/auth/sign-up-form.tsx) passes
  `options.data.locale` into `signUp`.
- Dashboard: trilingual "Confirm signup" template written to
  [`docs/4-integrations/email-templates.md`](../../4-integrations/email-templates.md) — **must be
  pasted into BOTH Supabase projects** (staging + production).

**C & D** (legal-page + guide translation) — deferred to post-walkthrough.

## Auth emails — full audit (2026-08-19)

After the signup email fix, audited the other auth email templates. Of the four
Supabase dashboard templates, **only two ever fire**:

| Template | Fires? | Action |
|----------|--------|--------|
| Confirm signup | ✅ | Localized (Fix A) |
| Reset password | ✅ | Localized — trilingual template in [`email-templates.md`](../../4-integrations/email-templates.md); paste into both dashboards |
| Invite user | ❌ dead | App sends its own via Resend → localized in code instead |
| Confirm email change | ❌ dead | No in-app email-change feature |
| Confirm reauthentication | ❌ dead | `reauthenticate()` never called |

**Code done this round:**
- ✅ `buildTeamInviteEmail` in [`lib/email.ts`](../../lib/email.ts) — team invite email now EN/KA/RU,
  language = inviter's org content locale; both call sites in
  [`lib/actions/invitations.ts`](../../lib/actions/invitations.ts) pass it. Tests:
  [`lib/__tests__/email-invite.test.ts`](../../lib/__tests__/email-invite.test.ts).
- ✅ Locale-sync: [`lib/actions/settings.ts`](../../lib/actions/settings.ts) `updateProfile` now mirrors the
  chosen UI language into `user_metadata.locale`, so `{{ .Data.locale }}`-driven
  emails (reset password) stay correct after a language change.

**Dashboard TODO (both projects):** paste the trilingual **Reset password** template.

## 4. Registration via Google (2026-08-19)

**Result:** 🟢 signup OK; 🔴 one fix-during (email defaults) + 🟡 one design item (calendar).

- Google OAuth sign-up worked; landed in dashboard. 🟢
- 🔴 **Default candidate-email templates showed in English despite a Georgian org.**
  Root cause: `runOnboarding` never seeded `organizations.default_content_locale`,
  so every org defaulted to `en`. Candidate emails resolve on **org content locale**
  (separate from the recruiter's UI locale by design), so defaults rendered English.
  The KA/RU default copy already exists in [`emails.source.json`](../../messages/emails.source.json).
  **Fixed:** onboarding now seeds the org content locale from the signup language —
  email signups from `user_metadata.locale`, OAuth from the onboarding `NEXT_LOCALE`
  cookie ([`lib/onboarding.ts`](../../lib/onboarding.ts), [`lib/actions/onboarding.ts`](../../lib/actions/onboarding.ts)).
  The "customized template linked to the org, no default afterwards" behavior was
  already correct (`saveEmailTemplate` + `resolveTemplate`). Existing prod test orgs
  (3) backfilled `en → ka`.
- 🟡 **Google Calendar not auto-linked after Google signup.** Decision: **post-signup
  prompt** (not forced calendar scope at signup). The Calendar integration is a
  separate OAuth (`/api/auth/google/callback`) storing tokens in the org integration;
  Google sign-in doesn't capture them. **Planned follow-up**, not yet built: a
  "Connect Google Calendar" prompt/banner after onboarding.

## 5. Registration via Microsoft (2026-08-19)

**Result:** 🟢 signup OK; 🔴 one copy fix (done) + 🟡 same integration follow-up.

- Microsoft OAuth sign-up worked.
- 🔴 **Onboarding button had stale copy** — `onboarding.openDashboard` ("Open dashboard")
  on `/onboarding/company`, but there is no dashboard page (onboarding redirects to
  `/pipeline`). **Fixed:** renamed key → `onboarding.openPortal` ("Open portal" /
  "პორტალის გახსნა" / "Открыть портал") in `messages/source.json` + component
  ([`components/onboarding/company-form.tsx`](../../components/onboarding/company-form.tsx));
  catalogs regenerated via `npm run messages:build`.
- 🟡 **Microsoft Teams + Outlook not auto-linked after MS signup.** Same category as the
  Google Calendar item — MS sign-in (`provider: 'azure'`, `scopes: 'email'`) doesn't
  request Teams/Outlook scopes or capture tokens. **Folded into the same
  post-signup "Connect integrations" prompt**, covering both providers:
  Google (Meet/Calendar) for Google signups, Microsoft (Teams/Outlook) for MS signups.

## Post-signup "Connect integrations" prompt — BUILT (2026-08-19)

Replaces the two 🟡 auto-link items above (decision: post-signup prompt, not forced
scopes at signup). A dismissible dashboard banner nudges OAuth users to connect the
integration matching their sign-in provider, until they connect or dismiss it.

- Decision logic (pure, tested): [`lib/integrations/prompt.ts`](../../lib/integrations/prompt.ts)
  — Google signup → Google Calendar/Meet; `azure` signup → Microsoft Teams/Outlook;
  email signups / already-connected / dismissed → nothing.
- Server component [`integration-connect-prompt.tsx`](../../components/integrations/integration-connect-prompt.tsx)
  short-circuits before any DB query (only OAuth + not-dismissed users incur the
  token lookup); rendered from the dashboard layout above page content.
- Client banner [`integration-connect-banner.tsx`](../../components/integrations/integration-connect-banner.tsx) —
  links to the existing `/api/auth/{google,microsoft}` connect flow; dismissal stored
  in the `int_prompt_dismissed` cookie (server-readable, no flash).
- i18n: `integrations.prompt*` keys (en/ka/ru). Tests: [`lib/integrations/__tests__/prompt.test.ts`](../../lib/integrations/__tests__/prompt.test.ts).
- Docs: [google.md](../../4-integrations/google.md), [microsoft.md](../../4-integrations/microsoft.md).

**Banner verified working (2026-08-19)** — appeared after Google signup. Clicking
**Connect** surfaced a **pre-existing** Google Calendar config bug, *not* a banner/code
issue (the banner links to the same `/api/auth/google` flow as Settings → Integrations).

## 🔴 Google + Microsoft OAuth — `redirect_uri_mismatch` (code bug — FIXED)

Both **Google Calendar** and **Microsoft Teams** "Connect" failed with a redirect-URI
mismatch on production. Both builders derive the redirect URI from the **same**
`NEXT_PUBLIC_SITE_URL`, so two providers failing identically pointed to a shared cause,
not two registration gaps.

**Root cause:** [`lib/google/calendar.ts`](../../lib/google/calendar.ts) and
[`lib/microsoft/graph.ts`](../../lib/microsoft/graph.ts) did **not** strip a trailing slash
from `NEXT_PUBLIC_SITE_URL` (unlike [`billing.ts`](../../lib/actions/billing.ts) +
[`calendly.ts`](../../lib/actions/calendly.ts), which already do). A prod value like
`https://hrhandle.com/` yields `https://hrhandle.com//api/auth/.../callback` (double
slash) → rejected by both Google and Azure.

**Fix (code):** new shared [`lib/site-url.ts`](../../lib/site-url.ts) `siteBaseUrl()` strips
trailing slash(es); both OAuth redirect builders now use it. Tested in
[`lib/__tests__/site-url.test.ts`](../../lib/__tests__/site-url.test.ts).

**Still verify after deploy** (in case the prod value isn't a trailing slash but `www.`/
`http`/wrong host): production `NEXT_PUBLIC_SITE_URL` should be exactly `https://hrhandle.com`,
and `GOOGLE_CLIENT_ID` / the Azure `client_id` must match the app where the redirect URIs
are registered. Requires a **redeploy** for the code fix to take effect.

### Update — redirect_uri resolved; now two time-based expiries (both CONFIG, not code)

Retested: both flows now get **past** the redirect_uri step. New errors, both explained by
"expired on their own" (matches "worked before, didn't change anything"):

- **Google → `Error 403: access_denied`** — "hrhandle.com has not completed the Google
  verification process… only developer-approved testers." The OAuth **consent screen is in
  "Testing" publishing status**; only listed test users can authorize, and Testing-mode
  refresh tokens also expire after **7 days**.
  **Fix (Google Cloud Console → APIs & Services → OAuth consent screen):** add the tester
  email(s) under **Test users** (fastest), or **Publish app** to production (the sensitive
  `calendar.events` scope may then require Google verification).
- **Microsoft → `token_exchange_failed`** (`msConnect.tokenFailed`) — authorize succeeded,
  the token exchange failed. Almost certainly an **expired Azure client secret** (secrets
  have expiry dates; a lapse breaks token exchange with no config change — AADSTS7000215/222).
  **Fix (Azure Portal → App registrations → app `d7ef9a54-…` → Certificates & secrets):**
  check the secret's expiry; if expired, create a new one and update `MICROSOFT_CLIENT_SECRET`
  on Vercel (prod), then redeploy. Confirm via Vercel logs: `[microsoft/graph] exchangeMicrosoftCode failed …`
  prints the exact `AADSTS…` code (secret vs redirect). Also confirm the redirect URI is
  registered under the **Web** platform on the Authentication tab.

### ✅ RESOLVED (2026-08-19) — both integrations connect

Definitive root causes from the logs / screens, both **config on the operator side** (no app
code fix needed beyond the trailing-slash hardening already shipped):

- **Microsoft** — `AADSTS7000215: Invalid client secret provided … is the client secret VALUE,
  not the client secret ID`. `MICROSOFT_CLIENT_SECRET` on Vercel held the secret **ID** (a GUID)
  instead of the one-time secret **Value**. Fix: new secret → copy the **Value** column → set on
  Vercel (prod) → redeploy. App ID + redirect URI were already correct. **Connected.**
- **Google** — after publishing, the "Google hasn't verified this app" screen for the sensitive
  `calendar.events` scope. **Not a block** — testers proceed via **Advanced → Continue**. Full
  Google verification (needs the Search Console domain-ownership TXT, added to Spaceship `@`) is
  only required to remove the warning for the public; deferred to launch. **Connected.**

**Two gotchas worth remembering (candidates for docs):** Azure **secret Value ≠ Secret ID**;
Google sensitive-scope testers use **Advanced → Continue**.

---

## Sign-up process — test coverage matrix

**Covered ✅ this session:**
- Email/password sign-up (KA) → confirm email → onboarding → pipeline
- Google OAuth sign-up → onboarding → pipeline; calendar connect (via prompt)
- Microsoft OAuth sign-up → onboarding → pipeline; Teams connect (via prompt)
- Confirmation email localized to signup language (KA)
- Org content locale seeded from signup language (default email templates in KA)
- Onboarding button copy (`Open portal`)
- Non-Latin org name → transliterated public slug

**Remaining / worth testing before calling sign-up "done" ⬜:**
| Pri | Case | Why |
|-----|------|-----|
| High | Duplicate email sign-up (already registered) | Confirm friendly error, no dup org |
| High | Invite-flow sign-up (`/join?token`) → joins existing org, not a new one | Localized invite email (KA/RU) + member_limit=2 trial |
| High | Onboarding⇄pipeline loop regression (OAuth, metadata stamp) | Load-bearing per CLAUDE.md |
| Med | Sign-up in RU + EN (email + content locale) | We only exercised KA end-to-end |
| Med | Password reset flow (forgot → email → reset) | Localized reset email template just pasted |
| Med | Confirmation link: expired / already-confirmed / cross-browser | token_hash flow |
| Low | Weak password / invalid email / missing captcha | Client + Turnstile validation |
| Low | Sanctioned-country gate → `/not-available` (G-008) | Needs VPN; edge |

### Duplicate-email sign-up — ✅ PASS (2026-08-19)

Signing up with an already-registered email shows the generic "confirmation email sent"
message and creates **no** duplicate account/org. This is intentional **anti-enumeration**
(Supabase returns a generic response; same posture as the password-reset flow per CLAUDE.md) —
correct, not a bug.

**Note — the "Security alert" email is Google's, not ours.** During this test the tester saw a
Google email ("You allowed HRHandle access…", from `no-reply@accounts.google.com`). That is
Google's standard OAuth-grant notification (from the earlier calendar connect), **not** the
HRHandle confirmation email, and its language is controlled by Google (recipient's account
language) — out of our scope. To verify *our* localized confirmation email, use a **fresh** email
with a Georgian UI and open the HRHandle "Confirm Your Signup" message specifically.

## 6. Vacancy creation — sector values untranslated → ✅ FIXED (2026-08-19)

The **Sector** dropdown in the create wizard showed English values (Financial, IT, …) in a
Georgian UI (only the "no sector" option was translated). Root cause: `sectors` is a global
lookup with English seed names rendered verbatim.

**Fix:** new [`lib/vacancies/sector-i18n.ts`](../../lib/vacancies/sector-i18n.ts) `sectorLabel(t, name)`
(mirrors `statusLabel`; keyed off the stable English seed name, falls back to raw name for
custom sectors). Applied at every user-facing sector display:
[step-basics](../../components/vacancies/wizard/step-basics.tsx) (create dropdown),
[step-review](../../components/vacancies/wizard/step-review.tsx) (summary),
[basic-info-section](../../components/vacancies/form/basic-info-section.tsx) (edit dropdown,
English name kept searchable), and the vacancies-list column. i18n keys `sector.*` (en/ka/ru).
AI-prompt / JD-snapshot uses **intentionally stay English**. Tests:
[`lib/vacancies/__tests__/sector-i18n.test.ts`](../../lib/vacancies/__tests__/sector-i18n.test.ts).

## 7. Vacancy creation — four issues (2026-08-19)

1. **🔴 ⋯ menu crash → FIXED.** The vacancy detail ⋯ menu threw *"Event handlers cannot be
   passed to Client Component props"* (`onSelect`). `VacancyHeader` is an **async server
   component** but passed `onSelect` to Radix `DropdownMenuItem`. Extracted the menu into a
   client component [`vacancy-actions-menu.tsx`](../../components/vacancies/detail/vacancy-actions-menu.tsx);
   header now renders it. Crash gone. **Menu polish** (follow-up): all three items
   (Edit / Duplicate / Delete) separated by dividers, **icon-less**, and fully translated —
   "Duplicate" was hardcoded English (now `vacancies.duplicate`); Duplicate/Delete restyled
   as plain ghost menu items. `DeleteVacancyButton` gained a `menuItem` prop so the Settings
   tab keeps its outline button.
2. **🟡 "Add" button untranslated → FIXED.** Screening-questions "Add" was hardcoded English;
   now `t('wizard.add')` ([step-scorecard](../../components/vacancies/wizard/step-scorecard.tsx)).
3. **🟡 "Full-time" untranslated on detail page → FIXED.** `formatEmploymentType` returned
   hardcoded English; now uses the `enum.employment.*` keys ([vacancies/[id]/page](../../app/(dashboard)/vacancies/[id]/page.tsx)).
4. **🟡 AI scorecard suggestions re-suggest existing / vary in count → IMPROVED.** The model
   was never told what's already on the scorecard, so it returned semantic near-dupes that
   survived the client's exact-match filter (→ only ~3 usable). Now the client passes
   `existing_skills` and the prompt excludes them + close rephrasings, and the rule asks for
   **exactly 5** ([`lib/ai/assessment-suggester.ts`](../../lib/ai/assessment-suggester.ts) +
   [route](../../app/api/ai/assessment-suggester/route.ts)). Prompt test:
   [`assessment-suggester-prompt.test.ts`](../../lib/ai/__tests__/assessment-suggester-prompt.test.ts).
   (AI counts are best-effort — a thin JD may still yield <5.)

## 8. Public apply flow — candidate-facing i18n (2026-08-19)

Tested on **production** (`hrhandle.com`). ⚠️ Reminder: production runs `main`; this
session's newest commits land there only after merge + a fresh Vercel build.

- **🟡 Application-confirmation email chrome untranslated → FIXED.** The subject/body were
  Georgian (from `emails.source.json`) but the wrapper text — "Thanks for Applying!",
  "Dear …", "Track your application", the keep-link-private note, and the "Sent via HRHandle"
  footer — was **hardcoded English** in [`lib/email.ts`](../../lib/email.ts). Added
  [`lib/email-i18n.ts`](../../lib/email-i18n.ts) `emailChrome(locale)` (en/ka/ru) and applied
  it to `sendApplicationConfirmationEmail`. Test: [`email-i18n.test.ts`](../../lib/__tests__/email-i18n.test.ts).
  **Follow-up:** the other candidate emails (status-change, offer, rejection, interview) share
  the same hardcoded chrome ("Dear", "Sent via HRHandle", CTAs) — not yet localized.
- **🔴 Public status/apply/offer pages rendered English despite a `ka` org → FIXED (real root
  cause).** Not a stale build. [`i18n/request.ts`](../../i18n/request.ts) `getRequestConfig`
  **ignored the requested locale** and only read the `NEXT_LOCALE` cookie — so every
  `getTranslations({ locale: contentLocale })` on the public candidate pages
  ([status](../../app/status/[token]/page.tsx), [apply](../../app/apply/[token]/page.tsx),
  [offer](../../app/offer/[token]/page.tsx)) was silently dropped, and an anonymous visitor
  (no cookie) always got English. Fix: honour `requestLocale` first, then the cookie, then
  English — extracted to pure `resolveRequestLocale()` in
  [`lib/i18n/locales.ts`](../../lib/i18n/locales.ts) (tested). This one change fixes all three
  public pages. Note: `format(…, 'MMM d, yyyy')` dates stay English (no date-fns locale) —
  separate, minor.

## Invite-flow sign-up (observed)

Accepting a team invite email redirects to the sign-up page with `?next=/join?token=…` and the
subtitle "Create an account to accept your invitation" — **expected** for a new invitee (they
create an account, then `acceptInvitation` joins the existing org). The invite sign-up UI shows
in **English** because a first-time invitee has no `NEXT_LOCALE` cookie yet — minor.

## 9. Post-apply testing round (2026-08-19)

1. **🟢 Profile changes in Settings** — work correctly. No action.
2. **🔴 Vacancy overview showed 0 in every pipeline bucket despite a real application →
   FIXED.** The overview funnel + attention metrics (active candidates, pending offers, new
   applicants) counted the legacy `applications.status_id`, which is **null on every current
   application** (they carry `pipeline_stage_id`). Now counts via
   `mapPipelineStageToBucket(stage)` → canonical bucket
   ([`app/(dashboard)/vacancies/[id]/page.tsx`](../../app/(dashboard)/vacancies/[id]/page.tsx)).
   Data was intact (verified on prod) — purely a broken read.
3. **🔴 Org logo upload failed ("ლოგოს ატვირთვა ვერ მოხერხდა") → FIXED.** The logo uploaded
   **client-side** to the `org-logos` bucket (blocked by storage RLS), unlike the avatar which
   uploads server-side. New `uploadOrgLogo` server action (admin client, owner/admin only) in
   [`lib/actions/settings.ts`](../../lib/actions/settings.ts); the form calls it
   ([`organization-form.tsx`](../../components/settings/organization-form.tsx)).
4. **🟡 Notifications not translated → follow-up (architectural).** `notifications.title/body`
   are stored as **pre-rendered English text** at creation ("New application: …", "Applied for
   …"). Since recipients can have different UI languages, proper i18n needs a **`type` + params
   (JSONB)** model rendered at **display time** — a migration + every `createOrgNotifications`
   call site + the notifications UI. Not done this round; recommended as a dedicated task.
5. **Dates on candidate-facing pages → FIXED (part of #1).** `format(…, 'MMM d, yyyy')` had no
   date-fns locale. New [`lib/i18n/date-locale.ts`](../../lib/i18n/date-locale.ts) `dateFnsLocale()`;
   applied to the status page dates.

**Still open from the previous round:** the *other* candidate emails (status-change, offer,
rejection, interview) still have hardcoded English chrome — `emailChrome()` can be extended, but
they carry more varied strings (interview table labels, offer/rejection CTAs) → a follow-up.

## 10. 2FA self-lockout + CI + pipeline i18n (2026-08-19)

- **🔴 CI/deploy failed → FIXED.** Duplicate `vacancies.duplicating` key in `source.json`
  (added with the ⋯-menu work; it already existed) failed the unique-keys test. Removed the dup.
- **🔴🔴 2FA self-lockout → FIXED (critical).** An owner who toggled the org **"require MFA"**
  policy **without enrolling** (`require_mfa=true`, `mfa_enrolled=false`) was bounced to
  `/settings/profile?enforce=mfa` on **every** navigation. But the enroll UI (`TwoFactorSection`)
  *and* the policy toggle live on **`/settings/security`**, which was **not** exempt — so the user
  could never reach the page to enroll or undo the policy → permanent lockout. Fix
  ([`lib/supabase/middleware.ts`](../../lib/supabase/middleware.ts)): the MFA enforcement now
  exempts and redirects to **`/settings/security`** (where enrollment + the toggle live).
  **Prod:** alexmerabishvili's org had `require_mfa=true`/`mfa_enrolled=false` — locked out until
  deploy or a manual policy reset.
- **🟡→✅ Pipeline card + quick-review i18n → FIXED.** (a) Relative time ("1h"/"Just now"):
  `timeInStage` now exposes `kind`+`value` and a `timeInStageLabel(t, …)` localizer
  ([`lib/pipeline/time-in-stage.ts`](../../lib/pipeline/time-in-stage.ts); `pipeline.timeJustNow/
  timeHours/timeDays`), used in cross-vacancy-card / list-view / terminal-rail. (b) **Source**
  ("Public Form"): stored as English text; new [`lib/pipeline/source-i18n.ts`](../../lib/pipeline/source-i18n.ts)
  `sourceLabel(t, raw)` maps app-generated values (`source.*` keys), free-text passes through;
  applied on the pipeline card + quick-review. (c) Quick-review **stage name** ("Screening") now
  localized via `statusLabel(t, code, name)`. Test: [`source-i18n.test.ts`](../../lib/pipeline/__tests__/source-i18n.test.ts).

## 11. Remaining i18n follow-ups — DONE (2026-08-19)

- **✅ Other candidate emails localized.** Extended `emailChrome()` and applied to
  interview-invitation, status-change, offer, and rejection emails (headings, greeting, CTAs,
  table labels, format labels, contact lines, footer; interview dates now use the org locale).
- **✅ Notifications i18n (architectural).** Added a `data jsonb` column
  ([migration](../../supabase/migrations/20260819_notifications_data.sql)); notifications now
  store structured params and are localized at **display time** in the recipient's UI language
  via [`lib/notifications/render.ts`](../../lib/notifications/render.ts) `renderNotification(t, …)`,
  falling back to the stored English `title`/`body` for pre-`data` rows. All 9 notification
  types wired; the bell ([`notifications-bell.tsx`](../../components/dashboard/notifications-bell.tsx))
  renders localized. `createOrgNotifications` tolerates the column being absent (retries without
  `data`), so it's **deploy-order-safe**. Note-mention body (the note preview) stays as-is.
  Tests: [`render.test.ts`](../../lib/notifications/__tests__/render.test.ts).

  **⚠️ APPLY MIGRATION** `20260819_notifications_data.sql` on **both** Supabase projects
  (staging + production) to activate localization — until applied, notifications fall back to
  English (no breakage).

---

# ✅ RE-TEST CHECKLIST — verify each after the `staging → main` deploy

Everything below is **fixed in code on `staging`**. Re-test on **production** once merged +
deployed. Legend: **✅** fixed (verify) · **⚙️** needs a manual dashboard/DB step · **⏳** deferred (not done).
Commit refs in parentheses.

## Onboarding & auth
- [ ] ✅ Email signup in a **Georgian** UI → **confirmation email in Georgian** (⚙️ Supabase "Confirm signup" template — pasted)
- [ ] ⚙️ Password reset → **email in Georgian** (⚙️ Supabase "Reset password" template — pasted)
- [ ] ✅ Non-Latin org name → **public URL slug** transliterated, not `-` (aada538)
- [ ] ✅ New org from a KA signup → **org content locale = ka** (drives candidate emails/pages) (5c4e9ca)
- [ ] ✅ OAuth first-time signup → `/onboarding/company` shows **"Open portal"** (not "Open dashboard") (e37768a)
- [ ] ✅ After onboarding → lands on `/pipeline` and **stays** (no onboarding⇄pipeline loop)
- [ ] ✅ Duplicate-email signup → generic message, **no duplicate org** (anti-enumeration)
- [ ] Invite-flow signup (`/join?token`) → **joins existing org**; invite email in inviter's org locale

## Public candidate pages (render in ORG content locale)
- [ ] ✅ Public **apply form** in KA (a82cba2 — requestLocale fix)
- [ ] ✅ **Status / track** page in KA, incl. **dates** (a82cba2, 4f6774c)
- [ ] ✅ **Offer** page in KA (a82cba2)

## Candidate emails (Resend) — all in org content locale
- [ ] ✅ Application confirmation — heading/greeting/CTA/footer (4e7875c)
- [ ] ✅ Interview invitation — heading, table labels, **date**, format, contact, footer (c50c29b)
- [ ] ✅ Status change (screening / interview) (c50c29b)
- [ ] ✅ Offer sent (c50c29b)
- [ ] ✅ Rejection (c50c29b)
- [ ] ✅ Team invite — inviter's org locale (11519c8)

## Vacancies & pipeline
- [ ] ✅ Vacancy create → **Sector** dropdown values localized (fcbff63)
- [ ] ✅ Vacancy detail **⋯ menu** opens (no "Event handlers…" crash); items separated, icon-less, all translated (3b69582, a1235ab)
- [ ] ✅ Screening **"Add"** button translated (3b69582)
- [ ] ✅ Vacancy detail **employment type** ("Full-time") localized (3b69582)
- [ ] ✅ AI scorecard suggestions exclude already-added + aim for 5 (3b69582)
- [ ] ✅ **Overview funnel counts** show the real applicant (not 0 across all buckets) (80a2a90)
- [ ] ✅ Pipeline card: **relative time** (1h/Just now), **source** ("Public Form"), stage names localized (a325b6f)
- [ ] ✅ Quick-review: source + "Advance to {stage}" localized (a325b6f)
- [ ] ✅ **#8** Openings-count field aligns with neighbours even with a wrapping KA label (a50eb28)
- [ ] ✅ **#10** Salary **currency defaults to the org's local/billing currency** (GE→GEL, EU→EUR, else USD), not hardcoded USD (a50eb28)
- [ ] ✅ **#9** Hiring manager = **searchable dropdown of org members**, UI shows only the name but stores a real FK (`hiring_manager_id`); works in create wizard **and** edit form (a698e3a) — ⚙️ migration `20260820_vacancy_hiring_manager_id.sql` on **both**

## Candidate profile
- [ ] ✅ **#1/#12** Source ("Public Form") localized in list + profile (earlier batch)
- [ ] ✅ **#11** AI-summary action buttons wrap instead of overflowing the card (a50eb28)
- [ ] ✅ **#2** **Activity feed localized** — application/note/document/interview headlines, stage/offer events, interview date + relative time all in the viewer's UI language (cd20e88) — ⚙️ migration `20260820_candidate_activity_i18n_params.sql` on **both**

## Apply form (public)
- [ ] ✅ **#7** AI-evaluation notice folded into the collapsed privacy section (not a separate prominent card) (a50eb28)
- [ ] ✅ **#13b** Placeholder/example text renders as a lighter gray hint (Input/Textarea, app-wide) (a50eb28)

## Auth / invite
- [ ] ✅ **#6** Invite set-password/name page renders in the inviting org's content locale (a50eb28)

## Notifications
- [ ] ✅ In-app notifications (bell) localized to viewer's UI language (eb9897a) — ⚙️ migration applied on both ✅

## Settings & security
- [ ] ✅ Profile changes save (was already PASS)
- [ ] ✅ **Org logo upload** works (server action) (2fc5a98)
- [ ] ✅ **2FA**: enroll on `/settings/security`; enabling "require MFA" before enrolling **no longer self-locks** (fa5b6e4); abandoned enroll doesn't block retry (7445eae); **QR renders clean & scannable** (fde4d1a)
- [ ] ✅ **OAuth redirect_uri** — no trailing-slash mismatch on Google/Microsoft connect (79a13ae)

## Integrations
- [ ] Post-signup **"Connect integrations" prompt** (Google Meet/Calendar; MS Teams/Outlook) appears + connects (e37768a)
- [ ] Google Calendar + Microsoft connect end-to-end (⚙️ config was fixed manually: Google test-users/publish, Azure secret Value)

## ⏳ Deferred (NOT fixed — future work)
- [ ] Legal pages (Terms / Refund / Privacy) translation
- [ ] Guide — content refresh + translation
- [ ] Notifications: only the app-side is localized; note-mention body (note text) stays as authored

# ✅ RE-TEST CHECKLIST — Round 2 (2026-08-20, after enabling Supabase CAPTCHA)

Turning on Supabase dashboard CAPTCHA broke two auth flows that weren't
forwarding a Turnstile token; both fixed. Plus a batch of i18n/UX gaps.

## Auth (⚠️ enabling Supabase CAPTCHA was the root cause of the first two)
- [ ] ✅ **#18** Change password with the **correct** current password succeeds (was falsely "current password is incorrect" — tokenless re-auth rejected by captcha) (7481aea)
- [ ] ✅ **#13a** Forgot-password actually **sends the email** (token now forwarded to Supabase, not consumed by our own check) (7481aea)
- [ ] ✅ **#14** Forgot-password success/error text localized (7481aea)
- [ ] ✅ **#16** Login/sign-up "Invalid login credentials" (+ other Supabase auth errors) localized (3302ff9)

## i18n / plan
- [ ] ✅ **#17** Invite past the plan limit → localized "reached your plan limit" toast (candidate/vacancy/member) (3302ff9)
- [ ] ✅ **#15** A Georgian-default org's **public careers page** (`/jobs/slug`) renders **Georgian** chrome (Full-time, Apply, Open positions); `/ka` `/ru` prefixes still force those (2b472db)

## Vacancies / candidates
- [ ] ✅ **#10** New vacancy salary currency defaults to **GEL** for a Georgian-language org with no billing country set (996ceba)
- [ ] ✅ **#11** Candidate profile: +Add (education/experience) + AI Regenerate buttons consistent; Documents upload row no longer overflows the rail (996ceba, 2b472db)

## ⚙️ Round-2 manual-step status
- [x] Supabase **CAPTCHA** enabled (Turnstile "HR Handle" widget) on **both** projects — this is what necessitated #18/#13a. Rule now in CLAUDE.md: **forward the Turnstile token to every Supabase auth call** (signup/signin/recover).
- [ ] After deploy, re-test the full password-reset round trip (request → email → set new password → login) on staging.

# ✅ RE-TEST CHECKLIST — Round 3 (2026-08-20, MFA/AAL2 + dates + Zoom publishing)

Tested on **production** (user merges `staging → main` and tests on `hrhandle.com`).

## Auth — MFA / AAL2 (evolved from Round-2 #18/#13a)
Enabling 2FA surfaced Supabase's AAL2 requirement + the prod
`GOTRUE_SECURITY_UPDATE_PASSWORD_REQUIRE_CURRENT_PASSWORD` setting.
- [ ] ✅ **#18 change-password (2FA on):** current password verified via the stateless `verifyPassword` server action (no AAL2 downgrade); if the session is AAL1 a **TOTP code step** appears; `updateUser({ password, current_password })` sends the current password. (8c5d42e, b7c784c, 75d5903)
- [ ] ✅ **#13a reset-password (2FA on):** email arrives, set-password prompts for a **TOTP code** (MFA challenge on the browser client → AAL2) then completes. Confirmed working. (8c5d42e, d95b356)
- [ ] ✅ **#19** forgot/reset/change submit buttons disabled until valid (no native "fill in this field" popup) (8c5d42e)
- [ ] ✅ MFA "enter your code" prompt renders as **neutral text**, not a red error (b7c784c)

## Dates
- [ ] ✅ **#20** audit-log timestamps numeric `dd.MM.yyyy HH:mm` (no English months) (2525dd2)
- [ ] ✅ **#21** native date inputs → localized **DatePicker** (`dd.mm.yyyy`/`დდ.თთ.წწწწ`/`дд.мм.гггг` hint, numeric display, localized calendar) — audit filters, offer form, vacancy wizard, stage offer block (2525dd2)

## #22 Zoom integration — connected + being published
- [ ] ✅ **Connect Zoom works.** Root cause was `getZoomRedirectUri` using raw `NEXT_PUBLIC_SITE_URL` (which ends in `/`) → `https://hrhandle.com//api/...` double slash → Zoom token exchange `Invalid Grant`. Fixed to use `siteBaseUrl()` like Google/MS (b40188c). Also: HEAD/prefetch guard on the callback (a0818ec); real error surfaced to the URL (`?zoom=error&reason=…&detail=…`) (80349f2, 08d2a25).
- [ ] ✅ **Deauthorization webhook** `/api/webhooks/zoom/deauthorization` (CRC + `x-zm-signature` + data-compliance), `profiles.zoom_user_id`, `ZOOM_SECRET_TOKEN` (9a998cb).
- [ ] ⏳ **Marketplace publish IN PROGRESS** — app config complete (redirect www, scopes `meeting:write:meeting`+`user:read:user`, deauth endpoint on `www`, App Listing, EU trader info, Technical Design). Submitting for Functional + Security review. Works for owner/test users until approved+published.

## ⚙️ Round-3 manual / config status
- [x] Zoom OAuth live on prod; `ZOOM_SECRET_TOKEN` set on prod Vercel (⚠️ **regenerate** it — it was pasted in chat — then update Vercel + redeploy).
- [x] `20260820_zoom_user_id.sql` applied on **prod**. ⬜ apply on **staging**.
- [ ] Zoom app: after Submit, wait for review (days–weeks) → auto-activates on approval.
- [ ] **#23 Node.js 20 → 24** on Vercel (builds disabled **Oct 1 2026**) — set Node 24 in project settings + bump `engines`/`.nvmrc`. Not urgent.
- [ ] **apex-vs-`www` cleanup (recommended):** `NEXT_PUBLIC_SITE_URL` is the **apex** with a **trailing slash**, but the site serves **`www`**. This caused a string of issues (Turnstile hostname, #15 canonical, Zoom webhook needing `www`, the Zoom redirect double-slash). Fix once: set `NEXT_PUBLIC_SITE_URL=https://www.hrhandle.com` (no trailing slash) + re-register Google/Microsoft/Zoom redirect URIs as `www`. See [[project_qa_retest_2026_08_19]].

## ⚙️ Manual-step status
- [x] Notifications migration `20260819_notifications_data.sql` applied on **both** envs
- [x] Supabase **Confirm signup** + **Reset password** templates pasted (both envs) — text unchanged since
- [ ] ⚠️ **Apply migration `20260820_vacancy_hiring_manager_id.sql` on both envs** (#9 hiring-manager FK). Code is deploy-order-safe (only writes the column when a manager is picked / reads it via a graceful query), so nothing breaks before it's applied — but the picker can't persist a link until it lands.
- [ ] ⚠️ **Apply migration `20260820_candidate_activity_i18n_params.sql` on both envs** (#2 activity i18n). `CREATE OR REPLACE VIEW`; page reads with `select('*')` so it's safe before/after. Until applied, the feed keeps rendering the English headline fallback.
- [ ] **#13a** Set `TURNSTILE_SECRET_KEY` on Vercel to activate apply-form captcha enforcement (the `[turnstile] … not set` line is a harmless fail-open warning, **not** a code bug)
- [ ] **Merge `staging → main` + deploy to production** ← gates every ✅ above

---

# Round 4 (2026-08-20) — new-modules batch (staging → prod re-test)

Ten items requested during PROD testing. All fixed on `staging`, full suite green
(1222 tests), production build clean. Re-test on **production** after merge.

- [ ] **#1 Node.js 20 → 24.** `engines.node`, `.nvmrc`, both CI jobs, docs (9ca77f6). ⚠️ **Manual:** set Node.js Version = 24 in Vercel Project Settings on BOTH envs.
- [ ] **#2 Single public language.** Org content language is now ONE dropdown (all 3 langs) in Settings → Organization; the multi-language public surface (`/[locale]/jobs`, hreflang, "also available" checklist) is removed. Public jobs live at `/jobs/[slug]` in the org's one language; legacy `/{locale}/jobs/*` 308-redirect to the bare path. Vacancy description forms auto-collapse to one language. No migration (reads derive from `default_content_locale`). (6f4510a)
- [ ] **#3 CV parser current position/company.** `backfillCurrentRole` derives them from the most-recent experience entry when the model returns null (cae5cb8).
- [ ] **#4 AI Summary removed** end-to-end (candidate profile rail + Review Mode + route + lib + privacy/compliance docs) (653d439). AI Fit Analysis improvement task list handed to the user (not built).
- [ ] **#5 Offer.** Recruiter-facing action errors localized (new `offerErr.*`); public offer page dates (date-fns `PPP` + org locale) + currency (Intl in org locale) + mailto subject localized; OfferForm modal gets `max-h-[90vh] overflow-y-auto` so the second-offer form can't clip (0e991fb).
- [ ] **#6 Screening checks.** Manually-added candidates (no apply-form answers) now show a neutral "No screening data" badge instead of a misleading green "All clear"; the candidate page passes the set of applications that actually have screening answers (5f67a38).
- [ ] **#7 Dashboard UI language on login.** Every login entry point (email/password, OAuth callback, email-confirm) now sets `NEXT_LOCALE` from the user's saved locale (`user_metadata.locale`, free), so logging in from a landing page in another language no longer leaves the dashboard in that language (51f8e2b).
- [ ] **#8 Rejection template display.** The Settings → Email templates view now shows the seeded "General" rejection template in the org's content language (matching what the email already sent), via `isDefaultTemplateContentAnyLocale` swap-on-read (90ca049).
- [ ] **#9 Interview reminders.** New daily cron `/api/cron/interview-reminders` (06:00 UTC) sends an in-app `interview_reminder` notification for interviews starting in ~26h (interviewer, or org owners/admins if unassigned); `reminder_sent_at` guards against double-send (718e42d). ⚠️ **Apply migration `20260821_interview_reminder.sql` on BOTH Supabase projects.** ⚠️ `CRON_SECRET` must be set on Vercel (already required by the other crons).
- [ ] **#10 Interviewer picker.** The email-collision filter no longer drops the current user, so the picker can't end up empty (single-member org / candidate sharing the recruiter's email) (9df2d10).

## ⚙️ Round-4 manual steps
- [ ] Set **Node 24** in Vercel (both envs) — #1. (`engines` now pinned to `24.x`; the build already runs on 24, so the dashboard change is optional/cosmetic.)
- [ ] Apply **`20260821_interview_reminder.sql`** on staging + prod — #9 (**required** — the cron errors on the missing `reminder_sent_at` column).
- [ ] Confirm **`CRON_SECRET`** is set on prod Vercel (gates the reminder cron).
- [ ] **#9 cron runs every 15 min** — needs a Vercel plan that allows sub-daily crons. If the deploy rejects `*/15 * * * *`, flag it.
- [ ] Merge `staging → main` + deploy; then walk #1–#10 on production.

## Round-4 re-test (2026-08-21) — after first prod deploy
Confirmed fixed by the user: **#2, #4, #7, #8, #10.** Re-fixed this round:
- **#1** — pinned `engines.node` `>=24` → `24.x` (stops Vercel's auto-upgrade warning) (1824872).
- **#3 + #6** — root cause was the public apply flow never persisting the parsed-CV
  profile (current role, salary, notice, location, timezone, languages) nor showing
  non-knockout screening answers. Apply form now posts `profile_json` → saved onto the
  NEW candidate; Screening panel shows EVERY answer (flagged ones amber) (1824872).
  ⚠️ Only NEW applications get the rail populated — existing candidates gain the full
  screening-answers list immediately but not the rail.
- **#5** — offer dates now `d MMMM yyyy` (no Georgian `-ე` ordinal); "7 days left"
  localized (`offer.daysLeft`); the **second offer is created inline**, not via the
  OfferForm modal popup (cc78a45).
- **#9** — reminder now fires ~1h before (every-15-min cron, ~75-min window) and
  respects the in-app "interview reminder" notification preference (e6e8e9e). Reverted
  to **daily** (afc3c00) because Vercel **Hobby** rejects sub-daily crons; setting copy
  reworded from "1 hour before" → "Ahead of an upcoming interview" (bd64aa0). True
  1h-before needs Pro (or a GitHub-Actions trigger).

---

# Round 5 (2026-08-21) — module deep-dive batch (staging → prod re-test)

Round-4 re-test confirmed fixed: **#1, #2, #3, #4, #5** (CV parse now fills the header
role/location/languages). Then a new-cases batch (N1–N9), all fixed on `staging`; full
suite 1227 green, prod build clean.

- [ ] **N1** — vacancy-detail status dropdown + work format ("On-site") were English; now
  localized via `vacStatus.*` / `enum.workMode.*` (5abf33a).
- [ ] **N2** — vacancy-overview "Review new candidates" link did nothing (`href="#"`); now
  points at `/vacancies/{id}/pipeline` (8c6fe48).
- [ ] **N3** — AI Fit Analysis error alert was English; localized via new `aiFitErr.*`
  (ac72591). The `[audit-log] fetch failed` log is a transient, non-fatal Supabase blip.
- [ ] **N4** — screening panel: dropped the static salary/notice/location tiles; now shows
  the real apply-form questions/answers as cards, hidden when empty, flagged ones amber
  (a9e472c).
- [ ] **N5/N6** — scorecard: "Submit scorecard" said "გაგზავნა" (send) in ka → "წარდგენა";
  the recruiter's submitted scorecard now shows a "Your scorecard" summary on the profile
  (98aa81e).
- [ ] **N7** — offer accepted/declined notification now carries the candidate name + links
  to `/candidates/{id}` (49a7ec8).
- [ ] **N8** — accepting an offer no longer auto-hires; profile shows an "Offer accepted 🎉"
  banner + explicit **Mark as Hired** button (`markApplicationHired`) (490bf5a).
- [ ] **N9** — the vacancy-edit screening-questions editor now reuses the create wizard's UX
  (Informational | Mandatory toggle + full passing-condition editor incl. number knockouts),
  via shared `PurposeButton`/`KnockoutConditionEditor`/`toKnockoutCondition` (b4356c9).

No new migrations this round. Merge `staging → main` + re-test N1–N9 on production.

---

# Round 6 (2026-08-21) — N1–N9 re-test + N10–N14 follow-ups

Re-test confirmed: **N1, N2, N5–N9 fixed.** **N3** is a Google-side Gemini **503**
(model overloaded) — feature + translation work, just retry. Follow-ups fixed (all on
`staging`, 1227 green, prod build clean; no new migrations):

- [ ] **N4b** — yes/no screening answer values were shown raw English → localized
  (common.yes/no), on both the candidate panel and Review Mode (2337f57).
- [ ] **N10** — removed the technical `model · prompt_v` line from the AI fit result UI;
  provenance stays on the `ai_fit_analyses` row for the audit trail (c503e2b).
- [ ] **N11** — Review Mode now shows the real apply-form screening answers (not static
  salary/notice), flagged ones amber (5c… `review(N11)`).
- [ ] **N12** — a scorecard for a vacancy with NO score attributes couldn't be submitted
  ("rate all attributes") → fixed; recommendation + reason submit on their own (c503e2b).
- [ ] **N13** — hid the useless Interview-stage "Reschedule" button when there's no
  interview to reschedule (c503e2b).
- [ ] **N14** — editing a DRAFT offer now uses the inline form pre-filled (updateOffer),
  not the modal popup; only a SENT offer shows the OfferPanel (2… `offer(N14)`).
- [ ] **Apply-link //** — the "Copy apply link" / apply-form tab / LinkedIn post built
  `https://host//apply/…` (double slash, NXDOMAIN on the apex) → strip the trailing slash
  from SITE_URL (c503e2b). ⚠️ The apex `hrhandle.com` still needs to resolve (DNS/Vercel)
  — the recommended `www` cleanup covers this.
