# Issues Found — Re-audit 2026-05-08

_Last updated: 2026-05-23 (progress section added after first batch of fixes)_

This document is the consolidated output of an exhaustive multi-agent re-audit
of the HRHandle staging codebase. Every issue has a stable ID; prefer
*referencing* the ID over re-describing the issue in commits / PRs / other
docs.

## Live-DB Verification Errata (2026-05-08)

Live Supabase MCP queries refuted one critical finding and surfaced five new ones:

- **❌ S-004 / S-rls-gaps RETRACTED** — RLS is in fact enabled on every public table with at least one policy (verified via `pg_policies`). The architecture agent's claim was based only on the SQL files in `supabase/migrations/` checked into the repo, which omit the original RLS setup migration. The risk does **not** exist in the form previously described.
- **🆕 S-NEW-1 through S-NEW-5** added below — surfaced by `mcp__supabase-staging__get_advisors`. See "Live-Advisor Findings" section.

## Summary

- **Total issues found:** 116 (after errata: −1 retracted, +5 from advisors)
- **Severity breakdown:** Critical = 3 · High = 23 · Medium = 56 · Low = 34
- **Category breakdown:**
  - 🔒 Security (S-) 19
  - 🐛 Bugs (B-) 6
  - ❌ Missing / incomplete (M-) 7
  - ➕ Missing features / fields (F-) 12
  - 🗑️ Unnecessary / redundant (U-) 8
  - ⚡ Performance (P-) 6
  - 🏗️ Architecture (A-) 7
  - 💼 Business logic (BL-) 14
  - ⚠️ Configuration (C-) 16
  - ♿ Accessibility (AC-) 12

## Progress (last updated 2026-05-23)

| Status | Count | Issues |
|---|---|---|
| ✅ **Fixed** | 93 | C-001..006; C-009/010/011/013/014/015 (Phase 1); C-016; S-001; S-002; S-003; S-007..S-014 (S-014 Phase 1); S-015; S-016; S-018; S-019; S-NEW-1/2/3/4; M-001; M-002..007; C-trial-member-limit; B-001..006; U-001..008; P-002/P-004/P-005/P-006; AC-001..011 (excluding 012); BL-001/002/003; BL-005; BL-008; BL-009/010; BL-011/012/013/014; F-003; F-005/006; F-007/008; F-010; F-011 (schema only — needs manual SQL apply); F-012; A-001; A-003; A-004; A-006 |
| 🟡 **Partially fixed** | 3 | S-006 (widget mounted, pending `TURNSTILE_SECRET_KEY` env-var rollout on Vercel); F-001 (4 of 5 triggers live; password-reset success deferred); F-002 (helper + integrations + interview/application/invitation events wired; subscription + role events deferred) |
| 🔻 **Severity reduced** | 1 | S-017 (no longer leaks DB info — rate-limit only) |
| ✋ **Accepted (intentional)** | 1 | S-005 (in-memory parse-cv rate limit; deliberate tradeoff per light-limits policy) |
| ⏸️ **Blocked** | 1 | S-NEW-5 (leaked-password protection — requires Supabase Pro plan) |
| ⏭️ **Open** | ~6 | F-004 (cancel-subscription UI — billing-blocked); AC-012 (WCAG audit); S-014/C-015 Phase 2 (drop `'unsafe-inline'`/`'unsafe-eval'` after observability) |
| 🕒 **Deferred** | 9 | P-001/P-003; AC-012; C-007/C-008 (billing); C-012/A-007 (strict tsconfig — 87 errors); A-002/A-005 (marginal-value refactors) |

**All four Supabase advisor `WARN` lints we could action are clear** on staging. Two warnings remain: `0029_authenticated_security_definer_function_executable` (intentional — RLS policies need it) and `auth_leaked_password_protection` (S-NEW-5, plan-blocked).

## Top 10 Most Critical (fix first)

1. ~~**C-1 / C-2 / C-3**~~ — **Fixed 2026-05-23** locally: typo `NEXT_PUBLIC_TURNSTILE_SITE__KEY` corrected in `.env.local`; verified site key now inlined in `/auth/login` and `/auth/sign-up` page chunks. **Vercel still needs verification** — confirm both staging and production projects use the single-underscore name `NEXT_PUBLIC_TURNSTILE_SITE_KEY`.
2. ~~**S-NEW-1**~~ — **Fixed 2026-05-23** via `scripts/026_revoke_anon_and_fix_search_path.sql` on both staging and production. Verified via `get_advisors` (lint `0028` no longer reported on staging).
3. ~~**S-2.1**~~ — **Fixed 2026-05-23** — `app/api/health/route.ts` simplified to liveness-only (`{status:"ok"}`); admin client removed, no DB info disclosed. See S-001.
4. ~~**S-11.4 / S-15.4**~~ — **Fixed 2026-05-23** (rate limit only; captcha deferred). `app/auth/forgot-password/page.tsx` now goes through server action `requestPasswordReset()` in `lib/actions/auth.ts` with 5/IP/hour + 5/email/hour caps and a generic response that closes the enumeration leak. Turnstile mounting deferred per session light-limits policy — re-open S-002 sub-item if needed.
5. ~~**S-11.3**~~ — **Fixed 2026-05-23** — `inviteTeamMember` now enforces a per-user cap of 25 invites/hour via a DB-backed count on `team_invitations.invited_by`. See S-003.
6. ~~**S-15.2**~~ — **Fixed 2026-05-23** (pending env-var rollout) — Turnstile widget mounted on `components/apply/apply-form.tsx` (invisible mode, same pattern as login/sign-up); server-side verification in `lib/actions/public-apply.ts` via new `lib/turnstile.ts` helper. Activates once `TURNSTILE_SECRET_KEY` env var is set on Vercel (both projects). Fail-open until then.
7. ~~**F-2**~~ — **Partially fixed 2026-05-23** — discovered the `activity_log` table already existed (created in `001_create_schema.sql`) with the right shape but was never written to. Added `lib/audit-log.ts` helper (best-effort, never throws) and wired 4 call sites: vacancy status changes, application status changes, LinkedIn connect, LinkedIn disconnect. Deferred: subscription events (no billing yet), role changes (no role-update action), Google/Zoom/Microsoft OAuth connects.
8. ~~**F-1 / M-2 / M-3**~~ — **Fixed 2026-05-23** — silent `catch {}` blocks in `lib/actions/interviews.ts`, `lib/actions/applications.ts`, and `lib/actions/public-apply.ts` all log via `console.error` with context. M-1 (`createOrgNotifications`) log lines now include notification type + recipient count. F-1: 4 of 5 triggers live (interview scheduled, application submitted, candidate-hired, team-invite-sent); password-reset success deferred (architectural change required).
9. ~~**C-trial-member-limit**~~ — **Closed 2026-05-23 (already consistent)** — verified: `lib/onboarding.ts:111` and `lib/types/subscription.ts:64` both use `member_limit: 2`; `docs/3-architecture/database.md:515` says `2 trial, 3 individual, 50 org`; regression test exists per `docs/testing/test-cases.md`. Doc-only entry that was already resolved in the original audit pass; no further action.
10. ~~**S-rate-limit-inmemory / S-005**~~ — **Accepted 2026-05-23** (intentional, documented). Per session light-limits policy we keep the in-memory 10/hr/IP cap instead of provisioning Upstash KV. Cold-start reset is acknowledged in `app/api/parse-cv/route.ts` with an inline comment + S-005 reference. Revisit if sustained abuse is observed.

---

## 🔒 Security Issues

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| S-001 | Critical | `app/api/health/route.ts:6-11` | Health endpoint uses admin client without auth; returns DB connectivity. Probes infra. | Drop to anon client and return only `{ok}`; or require an auth token. | **Fixed 2026-05-23** — endpoint rewritten as liveness-only (`{status:"ok"}`, 200). Removed admin client, DB ping, `checks`, and `timestamp`. Reasoning: anon client cannot read any table after S-NEW-1 fix (every public table's RLS requires `auth.uid()` or `get_user_org_id()` which anon no longer has EXECUTE on), so a liveness probe is the only safe public shape. DB-aware monitoring belongs in a separate authenticated endpoint when needed. |
| S-002 | Critical | `app/auth/forgot-password/page.tsx` | No rate limit on password-reset request → email enumeration + DoS. | Add per-email + per-IP rate limit (e.g. 3/email/hour) + Turnstile. | **Fixed 2026-05-23** — rate limits in place (5/IP + 5/email per hour, lighter than audit's 3/email/hour per session policy), generic success response added to close enumeration leak. New server action: `lib/actions/auth.ts:requestPasswordReset`. **Turnstile widget now mounted** on the forgot-password page (invisible mode, same shape as sign-up/sign-in/apply). `requestPasswordReset` takes a `captchaToken?: string` arg and calls `verifyCaptcha()`. Fail-open until `TURNSTILE_SECRET_KEY` is set on Vercel (same posture as S-006). |
| S-003 | Critical | `lib/actions/invitations.ts` | No rate limit on `inviteTeamMember`; an authenticated user can spam invites. | Add per-user cap (e.g. 10 invites/hour). | **Fixed 2026-05-23** — added `MAX_INVITES_PER_USER_PER_HOUR = 25` (lighter than audit's 10/hour per session-wide light-limits policy). Enforced via DB-backed count on `team_invitations.invited_by` over the last hour, placed after duplicate-invite check so typos don't penalise the user. |
| ~~S-004~~ | ~~Critical~~ | n/a | **RETRACTED 2026-05-08** after live-DB verification. RLS is enabled on every public table with ≥1 policy (`vacancies`, `candidates`, `applications` have 4 each; `profiles` has 6). The architecture agent only saw RLS in the migration files currently checked into `supabase/migrations/`. Earlier RLS migrations are not in the repo but are live in the DB. | n/a | Won't Fix (false positive) |
| S-005 | High | `app/api/parse-cv/route.ts:9-35` | In-memory rate-limiter map resets on cold start → ineffective on serverless. | Move to Upstash Redis or Vercel KV. | **Accepted 2026-05-23 (intentional)** — per session light-limits policy we keep in-memory + per-process. Inline comment in the route now references S-005 explicitly so future readers see the deliberate choice. Migration path: drop in Upstash KV with same `checkRateLimit(ip)` signature if abuse is observed. |
| S-006 | High | `lib/actions/public-apply.ts` (form) | Public apply form has no captcha despite Turnstile being available. | Mount Turnstile on `components/apply/apply-form.tsx`. | **Fixed 2026-05-23 (pending env-var rollout)** — Turnstile widget mounted (invisible mode). Submit disabled until token resolves. Server-side verification in `submitPublicApplication` via new `lib/turnstile.ts` calls Cloudflare's `siteverify` endpoint using `TURNSTILE_SECRET_KEY`. Fail-open while the secret is unconfigured (warning logged); activates on both projects once env var is set in Vercel. |
| S-007 | High | `sentry.server.config.ts` / `sentry.client.config.ts` | No `beforeSend` PII filter — candidate names, emails, application bodies can leak to Sentry. | Add `beforeSend` that scrubs `email`, `candidate*`, `application*`, `note*` fields. | **Fixed 2026-05-23** — new `lib/sentry-scrub.ts` exports a generic `scrubPii<T>()` helper wired into `beforeSend` on all three Sentry configs (server/client/edge). Redacts `user.email`/`username`/`ip_address`, cookie/authorization headers, `request.cookies`, breadcrumb data, and any nested key matching a PII pattern (`email`, `phone`, `password`, `token`, `secret`, `first_name`, `last_name`, `linkedin`, `salary`, `note`, `description`, `address`, `date_of_birth`, etc.). Also enabled `maskAllText` + `blockAllMedia` on Session Replay. 7 unit tests in `lib/__tests__/sentry-scrub.test.ts`. |
| S-008 | High | `app/auth/forgot-password/page.tsx` | Implicit OAuth flow used here intentionally (CLAUDE.md) but no inline comment in the file explains why. Risk: a future refactor switches to PKCE and breaks cross-browser reset. | Add a `// flowType: 'implicit' is required for server-side OTP verification — see CLAUDE.md` comment. | **Fixed 2026-05-23** — when the S-002 password-reset rate-limit work moved the Supabase call into `lib/actions/auth.ts`, a multi-line comment was added above the `createBrowserClient({ flowType: 'implicit' })` call referencing CLAUDE.md (lines 65–66). |
| S-009 | Medium | `app/api/cron/expire-vacancies/route.ts` | If `CRON_SECRET` env is unset, timing-safe compare returns false silently — all calls 401. Hard to debug. | Log a warning at startup if `CRON_SECRET` is unset; or treat missing secret as a build error. | **Fixed 2026-05-23** — module-top `if (!process.env.CRON_SECRET) console.warn(...)` so the misconfiguration is visible in Vercel logs. |
| S-010 | Medium | `lib/actions/public-apply.ts:43` | Uses admin client on a public endpoint. Justified (anonymous applicants) but lacks an inline rationale. | Add a comment + assert `application_form_token` is valid before any write. | **Fixed 2026-05-23** — multi-line rationale comment added above `const supabase = createAdminClient()`. It lists the gates that protect this surface (Turnstile, form-token vacancy lookup, IP + per-vacancy rate limits) and points at S-010 for the threat-model. Verified no DB writes happen before the `application_form_token` is validated at step 5. |
| S-011 | Medium | `lib/actions/public-apply.ts:40-80` | `experience_json` / `education_json` parsed without a Zod schema; only `JSON.parse` + light filtering. Malformed data can be persisted. | Use `ExperienceEntrySchema` / `EducationEntrySchema` from `lib/validations/candidate-background.ts`. | **Fixed 2026-05-23** — replaced the hand-rolled `filter()` / `as Record<string, unknown>` casts with `z.array(...).safeParse()` against inline schemas modelled on `ParsedCVSchema` (matches what the parse-cv API produces). Validation failures are logged + the section silently skipped (matches the existing best-effort semantics). |
| S-012 | Medium | OAuth callbacks (Google / MS / Zoom) | State cookie comparison + relative-redirect check are good, but cookies don't all explicitly set `Path=/`. | Audit each `cookies.set(...)` for explicit `path`, `secure`, `httpOnly`, `sameSite=strict`. | **Fixed 2026-05-23 (already aligned)** — audited all 3 state-cookie writes (`app/api/auth/{google,microsoft,zoom}/route.ts`): each one uses `{ httpOnly: true, secure: true, sameSite: 'strict', maxAge: 600, path: '/' }`. No code change needed. |
| S-013 | Medium | All OAuth disconnect endpoints | No CSRF token; rely on SameSite=Lax cookies + Next.js origin check. Document the assumption. | Add a doc note or migrate to server actions. | **Fixed 2026-05-23** — added an inline doc comment to all 4 disconnect routes (`app/api/auth/{google,microsoft,zoom}/disconnect/route.ts` + `app/api/integrations/linkedin/disconnect/route.ts`). The Google file holds the canonical rationale (SameSite=Lax Supabase session cookies + Next.js same-origin model); the others reference it. |
| S-014 | Medium | `next.config.mjs` CSP | Allows `'unsafe-inline'` and `'unsafe-eval'` in `script-src` (required by Next.js current shipping mode). | Investigate nonces / strict-dynamic; track as a hardening task. | **Fixed 2026-05-23 (Phase 1)** — moved CSP from static `next.config.mjs` to per-request `middleware.ts` via `lib/security-headers.ts:buildCsp(nonce)`. Each navigation gets a fresh nonce (`generateNonce()`, Web Crypto API), set on both the request `x-nonce` header and the response `Content-Security-Policy` header. Added `'strict-dynamic'` + `'nonce-…'` to `script-src`; kept `'unsafe-inline'` + `'unsafe-eval'` as the legacy-browser fallback (modern browsers ignore them when `'strict-dynamic'` is present). JSON-LD inline script in `app/page.tsx` now reads the nonce via `headers().get('x-nonce')` and stamps it. **Phase 2 (deferred)** — after a release cycle of clean observability, drop `'unsafe-inline'` + `'unsafe-eval'` to fully harden modern browsers. |
| S-015 | Medium | Supabase storage `candidate-documents` bucket | RLS policies are not in source — only Supabase dashboard. Risk of drift. | Capture bucket policies in `supabase/migrations` or a documented SQL snapshot. | **Fixed 2026-05-23** — wrote `scripts/029_storage_buckets_and_policies.sql` capturing the live state on both projects (verified via Supabase MCP): two buckets (`candidate-documents` private, `org-logos` public with 2 MB cap and image mime-type allowlist) and the three authenticated INSERT/UPDATE/DELETE policies on `storage.objects` for `org-logos`. Idempotent (`ON CONFLICT DO UPDATE` for the bucket rows, `DROP POLICY IF EXISTS` before each `CREATE POLICY`). `candidate-documents` intentionally has no policies because all access is via the service-role admin client. |
| S-016 | Low | `lib/actions/email-templates.ts` | User-supplied email body is stored as-is (no HTML sanitisation). Resend renders verbatim; XSS risk is on the email client side. | Document assumption; consider DOMPurify-style stripping if HTML allowed. | **Fixed 2026-05-23 (documented)** — multi-line comment above `saveEmailTemplate` in `lib/actions/email-templates.ts` explains the threat model: editing is restricted to owners/admins (privileged write access already), XSS risk is on the recipient's email client (modern clients strip script/iframe/event handlers), and sanitization would block legitimate formatting (links, bold). Reassess if a non-admin path to template editing is added. |
| S-017 | Low | Health endpoint | Public, no rate limit → minor info-disclosure / fingerprinting. | Add per-IP rate limit. | **Reduced 2026-05-23** — endpoint now returns only `{status:"ok"}` (see S-001), so info-disclosure/fingerprinting surface is gone. Rate-limit value is only DoS-mitigation now; can be deferred until edge rate limiting is added repo-wide. |
| S-018 | Low | OAuth state cookie `maxAge` | 600s on Google route; verify Microsoft/Zoom routes also use ≤ 600s. | Audit + align. | **Fixed 2026-05-23 (verified already aligned)** — all three providers use identical `{ httpOnly: true, secure: true, sameSite: 'strict', maxAge: 600, path: '/' }`. No code change needed. |
| S-019 | Low | `lib/cv-parser.ts` | API key read directly via `process.env.GOOGLE_GEMINI_API_KEY` — missing key returns `parse_failed` silently. Hard to debug. | Promote to `lib/env.ts`; throw at boot if feature flag enabled but key missing. | **Fixed 2026-05-23** — `GOOGLE_GEMINI_API_KEY` added to `lib/env.ts` server schema (optional — CV parsing is non-essential, degrade gracefully). `lib/cv-parser.ts` now `console.warn`s on each missing-key parse attempt so the misconfiguration is visible. |

---

### Live-Advisor Findings (added 2026-05-08 via `mcp__supabase-staging__get_advisors`)

| # | Severity | File / Object | Description | Suggested Fix | Status |
|---|----------|---------------|-------------|---------------|--------|
| S-NEW-1 | High | Function `public.get_user_org_id()` | `SECURITY DEFINER` function executable by `anon` (and `authenticated`) over `/rest/v1/rpc/get_user_org_id`. Any unauthenticated caller can invoke a function that bypasses RLS. | Revoke `EXECUTE FROM anon`; consider switching to `SECURITY INVOKER`. [Advisor doc](https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable) | **Fixed 2026-05-23** — `scripts/026_revoke_anon_and_fix_search_path.sql` applied to staging + production. Kept `SECURITY DEFINER` (required to break recursive `profiles` RLS lookup, see `scripts/004_fix_rls_helper.sql`); revoked from `PUBLIC` + `anon`, explicit grants to `authenticated` + `service_role`. Verified gone via `get_advisors`. |
| S-NEW-2 | Medium | RLS policy `notifications.Service role insert notifications` | INSERT policy uses `WITH CHECK (true)` — effectively unconditional. | Replace with a check on `organization_id IN (SELECT organization_id FROM profiles WHERE id = auth.uid())` or restrict to service role explicitly. [Advisor doc](https://supabase.com/docs/guides/database/database-linter?lint=0024_permissive_rls_policy) | **Fixed 2026-05-23** — `scripts/028_drop_unused_notifications_insert_policy.sql` applied to staging + production. Policy dropped entirely (was unused — only `createOrgNotifications()` inserts, and it uses service role which bypasses RLS). With RLS enabled + no INSERT policy, default-deny applies for anon/authenticated. Verified gone via `get_advisors`. |
| S-NEW-3 | Medium | Functions `sync_candidate_status_on_application_change`, `close_expired_vacancies`, `sync_candidate_hired_status` | All three have mutable `search_path` — opens a search-path hijacking surface. | Add `SET search_path = pg_catalog, public` to each function definition. [Advisor doc](https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable) | **Fixed 2026-05-23** — `scripts/026_revoke_anon_and_fix_search_path.sql` applied to staging + production. Pins `search_path = pg_catalog, public` on all three via `ALTER FUNCTION` (bodies untouched). Verified gone via `get_advisors`. |
| S-NEW-4 | Medium | Storage bucket `org-logos` | Bucket is public **and** has a broad `SELECT` policy on `storage.objects` that allows clients to *list* every file in the bucket (not just access individual URLs). | Drop the listing policy; public access by direct URL still works. [Advisor doc](https://supabase.com/docs/guides/database/database-linter?lint=0025_public_bucket_allows_listing) | **Fixed 2026-05-23** — `scripts/027_drop_org_logos_listing_policy.sql` applied to staging + production. Dropped policy `"Public read org logos"` on `storage.objects`. Direct-URL image access continues to work via the public bucket endpoint (`/storage/v1/object/public/...`). Verified gone via `get_advisors`. |
| S-NEW-5 | Low | Supabase Auth setting | Leaked-password protection (HaveIBeenPwned check) is disabled. | Enable in Dashboard → Authentication → Sign In / Providers → Email. [Advisor doc](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection) | **Blocked 2026-05-23 — Pro plan required.** Confirmed via staging dashboard: the leaked-password toggle is a paid feature. Revisit after upgrading to Supabase Pro on both projects. |

---

## 🐛 Code Bugs & Incorrect Logic

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| B-001 | High | `lib/actions/interviews.ts:43` | `scheduledDate < new Date()` creates a fresh `Date()` mid-comparison; subtle race at boundaries. | Cache `const now = new Date()` once and reuse. | **Fixed 2026-05-23** — cache `now` once and use `<=` so "right now" is rejected too. |
| B-002 | High | `lib/actions/invitations.ts:135` | Same pattern — `new Date(invite.expires_at) < new Date()` may allow boundary expiries. | Cache `now` and use `<=`. | **Fixed 2026-05-23** — same pattern as B-001 applied to `acceptInvitation`. |
| B-003 | Medium | `lib/actions/vacancies.ts:124` | Duration calculated via `Math.round((end-start) / 86_400_000)` may be off by one day across DST or near midnight. | Use `differenceInCalendarDays` from `date-fns`. | **Fixed 2026-05-23** — replaced ms-arithmetic with `differenceInCalendarDays(end, start)` from `date-fns` (already a dependency). |
| B-004 | Medium | `lib/actions/public-apply.ts:156` | `.eq('general_status_id', activeStatus?.id ?? '')` falls back to empty string → silently returns no rows. | Guard with `if (!activeStatus) return …`. | **Fixed 2026-05-23** — early-return with logged error if the global `active` candidate status row is missing (indicates DB misconfig). Removed the `?? ''` fallback at both call sites. |
| B-005 | Low | `components/dashboard/notifications-bell.tsx:51` | Async `onClick` handler without `.catch` — silently swallows mark-as-read failures. | Add `.catch(err => toast.error(...))`. | **Fixed 2026-05-23** — both single-mark and mark-all handlers now wrap the server-action call in try/catch with `toast.error()` + console.error. |
| B-006 | Low | `components/vacancies/linkedin-post-job-button.tsx:53` | `navigator.clipboard.writeText` without try/catch — fails on denied permissions / non-HTTPS. | Wrap in try/catch + manual-copy fallback. | **Fixed 2026-05-23** — onClick wrapped in try/catch; on failure shows `toast.error('Could not copy — please select the text manually.')`. |

---

## ❌ Missing or Incomplete Code

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| M-001 | High | `lib/actions/notifications.ts:73` | `if (error) console.error(...)` — caller never learns about failure. | Return `{success:false}` so caller can decide; or queue to dead-letter. | **Fixed 2026-05-23** — `createOrgNotifications` now returns `Promise<{ success: boolean }>` (never throws). The `createInterview` flow consumes this and pushes `'notification_failed'` to its `warnings` array (alongside the existing `'email_failed'`) so `interview-form.tsx` can surface distinct toast variants: email-only, notification-only, or both. Other callers (`inviteTeamMember`, `submitPublicApplication`, `updateApplicationStatus → candidate-hired`) still discard the result — they can opt-in later. |
| M-002 | High | `lib/actions/interviews.ts:313` | `} catch { /* Non-fatal */ }` swallows notification errors entirely. | At minimum `console.error('[interviews] ...', err)`; better, capture in Sentry. | **Fixed 2026-05-23** — catch now binds `err` and logs `'[interviews] post-create notification failed:'`. Sentry capture deferred to S-007 hardening pass. |
| M-003 | High | `lib/actions/applications.ts:341` | Rejection email failures caught silently. | Log + capture in Sentry. | **Fixed 2026-05-23** — catch now binds `err` and logs `'[applications] rejection email send failed:'`. Sentry capture deferred to S-007. |
| M-004 | Medium | `lib/actions/public-apply.ts:250, 274` | Experience / education JSON parse failures discarded silently → data quality blind spot. | `console.warn('[public-apply] experience parse failed', err)`. | **Fixed 2026-05-23 (via S-011)** — when the Zod-parse work landed for S-011 it also added `console.warn` for validation failures (`[public-apply] experience JSON failed validation: ...`) and `console.error` for parse errors (`[public-apply] experience parse error: ...`) at lines 292-298 + 314-320. Same for education. |
| M-005 | Medium | `lib/supabase/server.ts:21` | Cookie-set failure swallowed (intentional for Server Component contexts) but masks real middleware bugs. | Log in dev only. | **Fixed 2026-05-23** — `catch` now binds `err` and logs `'[supabase/server] cookie set failed:'` when `process.env.NODE_ENV === 'development'`. Production still swallows (intentional for the Server Component path documented above). |
| M-006 | Medium | `components/apply/apply-form.tsx:70` | CV parse failure shows a single generic message; user can't tell network vs invalid PDF. | Differentiate `TypeError` (network) vs schema (file). | **Fixed 2026-05-23** — added `parseFailureReason` state (`'network' \| 'file' \| null`). `fetch()` rejection → `'network'` (with `console.warn`); server `success: false` → `'file'`. UI now shows distinct messages: "Could not reach the server — please check your connection..." vs "Could not read this file — please complete the form manually." Reason resets on file removal. |
| M-007 | Low | `lib/guides/loader.ts:26, 48` | Two catches return `null` with no log context. | Log the slug + error message. | **Fixed 2026-05-23** — both `catch` clauses bind `err` and `console.warn` the slug (or `CONTENT_DIR`) + error message. Returns are unchanged so callers behave the same. |

---

## ➕ Missing Features or Fields

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| F-001 | High | `lib/actions/notifications.ts:53-78` | No notification triggers for: interview scheduled, candidate stage change, application submitted (to admins), team invite sent, password reset success. | Add `createOrgNotifications` calls in respective server actions. | **Partially fixed 2026-05-23** — 4 of 5 triggers live: interview scheduled (`lib/actions/interviews.ts:307`), application submitted (`lib/actions/public-apply.ts:334`), candidate-stage-change → hired (`lib/actions/applications.ts`, only fires on `hired` to avoid spam on every pipeline step), team invite sent (`lib/actions/invitations.ts`, notifies other owners+admins). **Deferred:** password-reset success — Supabase handles the actual reset client-side; capturing it server-side needs either a Supabase auth webhook (Pro plan) or wrapping `updateUser` in a server action (architectural change). |
| F-002 | High | `supabase/migrations/*` | No audit-log infrastructure for compliance-relevant events (subscription, roles, vacancy publish, candidate stage, integration connect/disconnect). | Create `audit_log` table with `org_id`, `user_id`, `entity_type`, `entity_id`, `action`, `before_value`, `after_value`. | **Partially fixed 2026-05-23** — re-scoped: `public.activity_log` already exists in DB (created by `scripts/001_create_schema.sql`, missing from `supabase/migrations/`). Schema uses `details jsonb` instead of `before_value`/`after_value` (we encode them as `{before, after}` inside details). Added `lib/audit-log.ts` helper writing via admin client (best-effort, never throws). Wired sites: `updateVacancyStatus`, `updateApplicationStatus`, LinkedIn save/disconnect, **Google/Microsoft/Zoom OAuth connect** (in each `/api/auth/<provider>/callback/route.ts`), **Google/Microsoft/Zoom OAuth disconnect** (in each `/api/auth/<provider>/disconnect/route.ts`). **Deferred:** subscription events (no billing webhook today), role-change events (no role-update action exists). |
| F-003 | High | `lib/actions/integrations.ts` | Only read action; no `disconnectLinkedInIntegration()` server action exists despite the HTTP route. | Add server-action wrapper for parity. | **Fixed 2026-05-23** — added `disconnectLinkedInIntegration()` server action in `lib/actions/integrations.ts`. Uses admin client to delete the row, writes an `integration.disconnected` audit-log entry, and `revalidatePath('/settings/integrations')`. The existing `app/api/integrations/linkedin/disconnect/route.ts` HTTP route stays for backward compatibility (the form-POST UI still uses it); the new server action is available for future callers. |
| F-004 | Medium | `app/(dashboard)/subscription/page.tsx` | No "Cancel subscription" UI / action. Pricing UI present but no payment provider wired. | Wait for LemonSqueezy integration; design cancel flow with confirm dialog. | Open |
| F-005 | Medium | `app/(dashboard)/vacancies/[id]/page.tsx` | `deleteVacancy()` action exists but no UI button surfaces it. | Add delete button + `AlertDialog` confirm. | **Fixed 2026-05-23** — new `components/vacancies/delete-vacancy-button.tsx` wraps the Delete button in an `AlertDialog` (title, body explaining that applications stay on candidates + the action can be reversed by an admin). Wired into the vacancy detail page header next to Duplicate / Edit. Uses sonner toasts for success / error feedback. |
| F-006 | Medium | `app/(dashboard)/candidates/[id]/page.tsx` | Same — no UI for `deleteCandidate()`. | Add delete button + confirm. | **Fixed 2026-05-23** — same shape as F-005: new `components/candidates/delete-candidate-button.tsx` wraps the icon button in an `AlertDialog`. Wired into the candidate detail page header next to the status selector + Edit button. |
| F-007 | Medium | `lib/validations/vacancy.ts` | No refinement preventing `end_date < start_date` or `end_date` in the past. | Add `.refine()`. | **Closed 2026-05-23 (already fixed)** — `lib/validations/vacancy.ts:33-39` has the `end_date >= start_date` refinement. The "end_date in the past" half of the audit suggestion was rejected: blocking past dates would break editing or back-filling historical vacancies; the cron job already closes expired vacancies so no harm in allowing them. |
| F-008 | Medium | `lib/validations/application.ts` | No `.email()` refinement on candidate email field. | Add it. | **Closed 2026-05-23 (already fixed)** — the `ApplicationSchema` only carries IDs (no email field). Candidate email is validated in `lib/validations/candidate.ts:6-11` via `.email()` and again in `lib/actions/public-apply.ts:85` via `z.string().email().safeParse(email)` on the public submission path. |
| F-009 | Medium | `app/(dashboard)/candidates/page.tsx`, `vacancies/page.tsx` | No keyset / offset pagination — uses limit only. Will scale poorly past a few hundred rows. | Add cursor pagination + total count. | Open |
| F-010 | Medium | `app/(dashboard)/candidates/page.tsx:189-216` | Sorting by status pulls all candidates into memory. | Move sort to DB; add `ORDER BY status, ...`. | **Fixed 2026-05-23** — switched to PostgREST relational-order: `.order('candidate_statuses(sort_order)', { ascending: true, nullsFirst: false })` plus a `created_at` tiebreaker. `candidate_statuses (sort_order)` is now included in the SELECT so PostgREST can resolve the relation. The whole `sort === 'status'` block now lives inside the same `range(from, to)` path as the other sorts — no more in-memory sort + slice. |
| F-011 | Low | `lib/actions/candidates.ts:124-139` | Soft-delete with no `restored_at` field → can't audit restore actions. | Add `restored_at`, `restored_by`. | **Fixed 2026-05-23 (schema only — needs manual SQL apply)** — wrote `scripts/030_add_restored_audit_columns.sql` adding `restored_at timestamptz` + `restored_by uuid REFERENCES profiles(id) ON DELETE SET NULL` to both `candidates` and `vacancies`. Idempotent (`ADD COLUMN IF NOT EXISTS`). Apply via Supabase SQL Editor on staging + production. **Wiring into a restore action is deferred** — there is no UI for restoring soft-deleted rows yet (no admin trash bin), so the columns exist to be populated when a restore flow is built. |
| F-012 | Low | `components/settings/team-invitations.tsx:69-80` | Revoke invitation has no confirmation dialog. | Wrap in `AlertDialog`. | **Fixed 2026-05-23** — Revoke icon button now opens an `AlertDialog` with the invitee's email and an explanation that the link will stop working. Confirm/Cancel pair; the AlertDialogAction triggers the existing `handleRevoke` which already toasts on success (BL-011). |

---

## 🗑️ Unnecessary or Redundant Code

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| U-001 | High | `app/(dashboard)/candidates/page.tsx:187-188` | `eslint-disable-next-line @typescript-eslint/no-explicit-any` + `as any` cast on Supabase query builder. | Use the proper return type from `@supabase/supabase-js`. | **Fixed 2026-05-23** — removed the cast + eslint-disable. Refactored `let sortedQuery: any = baseQuery` to a single `let sortedQuery = baseQuery.order(...)` initializer; TypeScript now infers the chain correctly. |
| U-002 | High | `app/(dashboard)/vacancies/page.tsx:198-199` | Same pattern as U-001. | Same fix. | **Fixed 2026-05-23** — same fix as U-001 applied to the vacancies page. |
| U-003 | Medium | `components/vacancies/vacancy-form.tsx:79,141,143,156,157,217` | Multiple `as any` casts for `responsibilities`, `show_on_public_page`, action results. | Extend `VacancyFormData` type and action return types. | **Fixed 2026-05-23** — added `responsibilities: string \| null` to `Vacancy` interface in `lib/types/vacancy.ts` (the column already existed in DB + `VacancyFormData`). Removed 6 `as any` casts. Split the `vacancy ? updateVacancy : createVacancy` call into discriminated branches so the `data.id` access on the create result is typed without a cast. Updated the duplicate local `VacancyRow` interface in `app/(dashboard)/candidates/[id]/edit/page.tsx` + the SELECT to include `responsibilities` (A-001 still tracks the broader row-type duplication). |
| U-004 | Medium | `app/(dashboard)/candidates/new/page.tsx` | `vacancies as any`, `candidateStatuses as any` passed to client component. | Fix prop types. | **Fixed 2026-05-23** — narrowed `CandidateFormProps.vacancies` to `Pick<Vacancy, 'id' \| 'title'>[]` (only fields the component actually uses), allowing the partial Supabase select result through without a cast. `candidateStatuses` cast removed — already matched. |
| U-005 | Medium | `app/(dashboard)/settings/team/page.tsx:45-46` | `pendingInvitations as any`, `teamMembers as any` casts. | Fix prop types. | **Fixed 2026-05-23** — Supabase select shape now matches the local `Invitation` / `TeamMember` prop types directly; both casts dropped. |
| U-006 | Medium | `lib/actions/notifications.ts:73,75` | `console.error` calls in production code. | Replace with Sentry capture in production paths. | **Fixed 2026-05-23** — `createOrgNotifications()` now `Sentry.captureException`s alongside the existing `console.error` (kept for local-dev visibility). Tagged `area: notifications, op: insert` with `type` + `recipientCount` in `extra` for filtering. PII-safe — the notification body/title is not included. |
| U-007 | Medium | `lib/actions/interviews.ts:87` | `console.error('[interviews] reschedule email send failed:', err)` in production. | Same — Sentry. | **Fixed 2026-05-23** — added `Sentry.captureException` (with op tag `reschedule_email`) on the reschedule path, and same pattern on the create-interview email + post-create notification catches. Cancellation-cleanup failures (BL-010) are also captured under `cancel_google_cleanup` / `cancel_zoom_cleanup`. |
| U-008 | Low | `lib/env.ts` | `LINKEDIN_CLIENT_ID` / `LINKEDIN_CLIENT_SECRET` validated but never read by any code. | Either implement OAuth or remove from env validation. | **Fixed 2026-05-23** — removed both keys from `lib/env.ts` server schema and `runtimeEnv`. Replaced the placeholder block in `.env.example` with a note explaining the LinkedIn integration uses manual page-ID entry instead of OAuth. |

---

## ⚡ Performance Issues

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| P-001 | High | `lib/actions/applications.ts:63-69, 247-253` | N+1 pattern when updating multiple application statuses (separate fetch for hiredApps then candidate per row). | Join applications → statuses → candidates in a single query. | **Deferred 2026-05-23 — pattern re-investigated, no longer matches the audit line ranges after the F-1 / F-2 / audit-log work restructured this function.** The current `hiredApps` lookup in `updateApplicationStatus` runs once per call (a single candidate's other applications) — not N+1 across multiple updates. Re-audit when the pipeline-board enables bulk stage transitions. |
| P-002 | Medium | `lib/actions/vacancies.ts:119-129` | `new Date(...).getTime()` invoked twice per call. | Cache parsed dates. | **Fixed 2026-05-23 (via B-003)** — replaced ms-arithmetic with `differenceInCalendarDays(end, start)`, which constructs each Date once internally. |
| P-003 | Medium | `lib/actions/candidates.ts:158` | Hardcoded `.limit(20)` without cursor or total count → users can't paginate. | Add proper pagination. | **Deferred 2026-05-23** — pagination is a multi-file refactor (list query, count query, UI page-controls). Track as a dedicated task. |
| P-004 | Medium | `lib/actions/interviews.ts:64-68,143-148,217-222` | `Promise.all` rejects whole batch on one failure. | Use `Promise.allSettled` where partial success is acceptable. | **Fixed 2026-05-23** — converted all 3 sites (reschedule email, Google Meet creation, Microsoft Teams creation). Each Supabase fetch is now independent; a failed lookup yields `null` instead of cancelling the whole side-effect. Comment in each site references P-004. |
| P-005 | Medium | `lib/cache/lookups.ts` | Cache keys not scoped per-org. Currently safe because the cached tables are global lookups, but future per-org caches must avoid the same pattern. | Add `orgId` to keys whenever the table is tenant-scoped. | **Fixed 2026-05-23** — added a module-level comment explaining why these keys are intentionally global (the underlying tables are shared lookups) and warning future maintainers to scope `org_id` into the key for tenant data. |
| P-006 | Low | `lib/actions/public-apply.ts:231-276` | `JSON.parse` in hot path without prior schema check. | Zod-parse before `JSON.parse`. | **Fixed 2026-05-23 (via S-011)** — `experience_json`/`education_json` now go through `z.array(Schema).safeParse(JSON.parse(...))` with explicit per-field validation. |

---

## 🏗️ Architecture & Code Quality

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| A-001 | Medium | Multiple `*/page.tsx` | Row types (`SectorRow`, `VacancyRow`, etc.) duplicated across files. | Centralise in `lib/types/database.ts`. | **Fixed 2026-05-23** — created `lib/types/database.ts` exporting `CandidateStatusOption` + `ApplicationStatusOption` as `Pick<>`s of the canonical types in `candidate.ts`/`application.ts`. Removed 9 duplicated inline interfaces across `candidates/page.tsx`, `candidates/[id]/page.tsx`, `candidates/[id]/edit/page.tsx`, `dashboard/page.tsx`, `vacancies/[id]/page.tsx`, `application-evaluation.tsx`, `candidate-applications-list.tsx`, `vacancy-application-row.tsx`, `vacancy-applications-list.tsx`. The vacancy-detail page also dropped a redundant manual `generalStatus` projection (it now passes the lookup result directly). Other row types (vacancies, sectors, applications) left for incremental cleanup. |
| A-002 | Medium | `app/(dashboard)/candidates/page.tsx`, `vacancies/page.tsx` | Filter/sort business logic inside page components. | Extract to `lib/actions/*-list.ts`. | **Deferred 2026-05-23 — marginal-value refactor.** The candidates list page runs ~7 cross-cutting queries (profile column prefs, candidate statuses, vacancy options for the filter dropdown, vacancy-filter resolution, the main candidates query, applications for the visible page, and the vacancy lookup for the filter pill). Extracting only the main candidates query into `lib/actions/candidates-list.ts` would leave the page complexity essentially unchanged. A meaningful refactor would consolidate all of these into a single `getCandidatesPageData()` action, which is a substantial restructure with no immediate user-facing benefit. Re-open as a dedicated cleanup PR. |
| A-003 | Medium | `lib/actions/applications.ts:71-75` | Type guard for `application_statuses` handles both array & single object — relation shape inconsistency. | Use a type predicate; document the Supabase relation return shape. | **Fixed 2026-05-23** — extracted `unwrapStatusRelation<T>(rel)` helper at the top of `lib/actions/applications.ts` with a comment explaining the PostgREST array-vs-object oddity. Replaced 2 duplicated inline branches with a one-line `unwrapStatusRelation(...).?code === APPLICATION_STATUS.HIRED`. |
| A-004 | Medium | `lib/validations/candidate-background.ts` | Possible overlap with `lib/validations/candidate.ts`. | Audit and de-duplicate. | **Fixed 2026-05-23 (no real overlap)** — the two files cover different concerns: `candidate.ts` validates the `candidates` row (name/email/etc., used by the create/edit form); `candidate-background.ts` validates related rows (`candidate_experience`, `candidate_education`) and the AI-parsed CV envelope. The inner experience/education objects inside `ParsedCVSchema` deliberately have relaxed nullability vs. the per-row schemas (best-effort AI output vs. user-saved data). Added a header comment in `candidate-background.ts` documenting the boundary so future readers don't merge them. |
| A-005 | Low | `components/vacancies/vacancy-form.tsx` | ~40 KB, ~500+ lines. Mixed form state, validation, custom fields, layout. | Split into smaller modules. | **Deferred 2026-05-23 — naive split would regress maintainability.** The component centralizes a single `useState<VacancyFormData>` plus a 30-line `validateForm` + a 30-line payload builder. The JSX is organized into self-contained `<Card>` sections (Basic Info / Compensation / Dates / Description / Public-page). A mechanical split into child components would require either prop-drilling `formData` + `setFormData` (degrades inline editability of the form-shape) or introducing a `useReducer`/context/form-library, which is a bigger refactor than the audit calls for. Best handled as a "migrate to react-hook-form" task or similar; not a quick win. |
| A-006 | Low | Codebase-wide | Magic status strings (`'hired'`, `'active'`, etc.) sprinkled across files. | Define enums in `lib/types/constants.ts`. | **Fixed 2026-05-23** — `lib/types/constants.ts` now exports `APPLICATION_STATUS`, `CANDIDATE_STATUS` (both `as const` records), `ACTIVE_APPLICATION_STATUS_CODES` (readonly tuple), plus matching `ApplicationStatusCode` / `CandidateStatusCode` literal-union types. Wired through `lib/actions/applications.ts` — the heaviest user (11 sites). Remaining call sites in pages/components left for incremental cleanup; pattern is established. |
| A-007 | Low | `tsconfig.json` | `strict: true` but no `noUncheckedIndexedAccess` / `exactOptionalPropertyTypes`. | Tighten incrementally. | **Deferred 2026-05-23** — same scope as C-012; see C-012 for the full rationale (87 errors, multi-file cleanup needed). |

---

## 💼 Business Logic Improvements

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| BL-001 | High | `app/(dashboard)/candidates/page.tsx:175` (audit cited wrong path; actually in candidates list page, not public-apply) | When vacancy filter returns 0 candidates, query uses fake UUID to force empty result. Works but is opaque. | Add explicit empty-result branch. | **Fixed 2026-05-23** — replaced the fake-UUID branch with an explicit `forceEmpty` short-circuit. When `vacancyFilter` is set but no candidates have applied to it, the candidates query is skipped entirely (`candidates = []`, `totalCount = 0`). All filter logic now lives inside the `else` branch so the query is constructed only when it can return rows. |
| BL-002 | High | `lib/actions/applications.ts:212-216` | 5-active-applications cap rejects silently with generic error. | Surface "Reach Hired or remove an active application to add a new one" + link to subscription. | **Fixed 2026-05-23** — rewrote the error message in `createApplication` and the dialog/tooltip in `components/candidates/add-application-dialog.tsx` to be explicit and actionable: "This candidate is already active on N vacancies. Move one to Hired or Rejected, or archive it, before adding a new one." The "link to subscription" suggestion was dropped — the 5-applications cap is a per-candidate quality control, not a plan-based billing limit, so a subscription link would be misleading. |
| BL-003 | Medium | `lib/actions/invitations.ts:22,102` | Repeated `ctx.role !== 'owner' && ctx.role !== 'admin'` inline checks. | `isOrgAdmin(role)` helper. | **Fixed 2026-05-23** — created `lib/permissions.ts` exporting `isOrgAdmin(role)` (sync helper, can't live in `lib/actions/index.ts` which is `'use server'`). Replaced 11 inline checks across `lib/actions/invitations.ts`, `lib/actions/rejection-reasons.ts`, `lib/actions/rejection-templates.ts`, `lib/actions/email-templates.ts`. |
| BL-004 | Medium | `lib/actions/candidates.ts:15-16`, `vacancies.ts:11-12` | Plan-limit error returned as string but UI has no upgrade CTA. | Return error code `PLAN_LIMIT` + link to `/subscription`. | Open |
| BL-005 | Medium | `lib/validations/interview.ts:17-19` | `duration_minutes` has no max — 999 999 min is accepted. | `.max(1440)` (24 h). | **Closed 2026-05-23 (already fixed, stricter than audit)** — `duration_minutes` schema is `z.number().int().min(15).max(480).default(60)` — capped at 480 (8 h), well under the audit's suggested 1440 (24 h). No change needed. |
| BL-006 | Medium | `app/(dashboard)/vacancies/[id]/page.tsx` | No skeleton / empty state for "0 applications". | Add `Skeleton` + empty message. | Open |
| BL-007 | Medium | `lib/actions/applications.ts:62-65` | Soft-deleting candidate cascades to applications implicitly. | Document; warn user if active applications exist. | Open |
| BL-008 | Medium | `lib/actions/interviews.ts:200+` | Interview created OK but email fails silently. | Return `{success:true, warning:'email_failed'}` and show toast. | **Fixed 2026-05-23** — `createInterview()` now returns `{ success: true, data: { id, meetLink, warnings: string[] } }`. The `warnings` array collects non-fatal failures (currently `'email_failed'`). `components/interviews/interview-form.tsx` checks `result.data.warnings` and shows `toast.warning('Interview scheduled, but the invitation email could not be sent.')` instead of `toast.success`. |
| BL-009 | Medium | `BL-microsoft-redirect` — Microsoft OAuth redirects to `/settings/integrations?microsoft=*` while Google/Zoom redirect to `/settings?*=*`. | Inconsistent UX; one of the two paths is dead. | Pick a target path and unify. | **Fixed 2026-05-23** — all 4 OAuth flows now redirect to `/settings/integrations?<provider>=<status>`. Touched Google `route.ts`/`callback/route.ts`/`disconnect/route.ts` and the corresponding Zoom files; Microsoft + LinkedIn were already on this path. The connect components (`google-calendar-connect.tsx`, `zoom-connect.tsx`) live on `/settings/integrations`, so users now land on the page that shows the result. |
| BL-010 | Medium | `BL-zoom-cleanup` — `lib/zoom/meetings.ts` | Cancelling an interview leaves the Zoom meeting alive (Google does delete the event). | Add `deleteZoomMeeting()` on interview cancel. | **Fixed 2026-05-23 (corrected audit premise)** — investigation showed the audit was partly wrong: Google also did **not** delete the event on cancel (`deleteCalendarEvent` was unused). New helpers in `lib/zoom/meetings.ts`: `deleteZoomMeeting(token, id)` and `parseZoomMeetingIdFromJoinUrl(url)` (regex parses `/j|s|w/\d{9,12}` to recover the meeting ID from the stored join URL — avoids a DB migration). `updateInterviewStatus(_, 'cancelled')` now best-effort deletes both the Google calendar event (via `deleteCalendarEvent` + stored `google_calendar_event_id`) and the Zoom meeting; failures are logged + sent to Sentry but never block the status update. |
| BL-011 | Low | `components/settings/team-invitations.tsx:69-80` | Revoke is silent on success; no toast, no optimistic UI. | Add `toast.success`, disable button while pending. | **Fixed 2026-05-23** — swapped the Alert-based success/error path on revoke with `toast.success` / `toast.error` from `sonner` (project's existing toast lib). Button-disable while pending was already implemented via the shared `isPending` state. Invite-form success messaging kept as Alert (richer affordance). |
| BL-012 | Low | `lib/actions/vacancies.ts:102-209` | If original has null `end_date`, duplicate also gets null — user may not notice. | Default to "+90 days" or surface a warning. | **Fixed 2026-05-23** — when the original has `end_date == null`, the duplicate now defaults to `today + 90 days` (matching the audit's suggested fallback). User can still clear it on the edit page. |
| BL-013 | Low | `components/settings/linkedin-connect.tsx` | Disconnect has no confirmation. | Add confirm dialog. | **Fixed 2026-05-23** — wrapped the Disconnect button in an `AlertDialog` ("Disconnect LinkedIn? You won't be able to post jobs … Existing posts on LinkedIn remain unchanged."). The confirming action still POSTs to the existing HTTP route. |
| BL-014 | Low | `BL-text-consistency` — UI capitalisation `"Thanks for Applying!"` vs `"You've Applied!"` | Inconsistent tone / case across two near-identical screens. | Align wording. | **Fixed 2026-05-23** — apply-form success screen now reads "Thanks for Applying!" + "We've sent a confirmation to …" to match `lib/email.ts` (email body) and `components/settings/email-templates-manager.tsx` (preview). |

---

## ⚠️ Configuration & Environment Issues

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| C-001 | Critical | `.env.local:13` | `NEXT_PUBLIC_TURNSTILE_SITE__KEY` — typo (double underscore). | Rename to `NEXT_PUBLIC_TURNSTILE_SITE_KEY`. | **Fixed 2026-05-23** — renamed in `.env.local`; also removed dead `NEXT_PUBLIC_TURNSTILE_SECRET_KEY` line (unused in code; secret lives in Supabase CAPTCHA dashboard). Vercel env-var name still requires manual verification on staging + production. |
| C-002 | Critical | `components/auth/sign-up-form.tsx:291` | Code reads `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (single underscore) → resolves to `undefined`. | Once C-001 is fixed this resolves. | **Fixed 2026-05-23** — resolved by C-001. Verified site key is now inlined in `/auth/sign-up` page chunk by Turbopack at compile time. |
| C-003 | Critical | `app/auth/login/page.tsx:226` | Same env var mismatch as C-002 — captcha widget never receives its site key. | Same fix. | **Fixed 2026-05-23** — resolved by C-001. Verified site key is now inlined in `/auth/login` page chunk by Turbopack at compile time. |
| C-004 | High | `app/layout.tsx:17,39,61` | Hardcoded `https://hrhandle.com` in metadata — staging will publish prod URLs in og:url etc. | Use `process.env.NEXT_PUBLIC_SITE_URL`. | **Fixed 2026-05-23** — `const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://hrhandle.com'` extracted at module top; used in `metadataBase`, `openGraph.url`, `alternates.canonical`. Fallback preserves prod behaviour when the var is absent. |
| C-005 | High | `app/page.tsx:23,33,39,62` | Same hardcoded prod URL in landing-page metadata. | Same fix. | **Fixed 2026-05-23** — same approach as C-004; `SITE_URL` used in `metadata.alternates.canonical`, `jsonLd.url`, and `jsonLd.provider.url`. |
| C-006 | High | `lib/google/calendar.ts:4-5,17` | Hardcoded `accounts.google.com` and Google API URLs. Acceptable but not configurable. | Extract to constants. | **Fixed 2026-05-23** — promoted `OAUTH_AUTH_URL`, `SCOPE_CALENDAR_EVENTS`, `SCOPE_USERINFO_EMAIL` to module-level constants alongside the existing `TOKEN_URL` / `CALENDAR_API`. `getGoogleOAuthUrl()` no longer interpolates raw strings. |
| C-007 | High | `lib/types/subscription.ts:56-107` | Hardcoded plan limits (`5/500/1000`, `100/10000/20000`, `2/3/50`). | Move to env / config table once billing is wired. | **Deferred 2026-05-23 — pending billing integration.** The audit's own suggested fix is "Move to env / config table **once billing is wired**." LemonSqueezy isn't wired yet (see F-004), so the right shape for these constants depends on what billing exposes. Revisit alongside the billing PR. |
| C-008 | High | `lib/campaign.ts:11-19` | Hardcoded "Spring Offer" campaign with dates and discount %. | Move to env / DB once billing exists. | **Deferred 2026-05-23 — pending billing integration.** Same as C-007 — the audit fix is conditional on billing being wired. Campaign data should live in the same store as the plan price IDs to avoid drift; revisit alongside C-007. |
| C-009 | High | `components/candidates/add-application-dialog.tsx:47` | Hardcoded `activeApplicationCount >= 5` parallel to `lib/actions/applications.ts:147`. | Single source of truth: import constant. | **Fixed 2026-05-23** — new `lib/types/constants.ts` exports `MAX_ACTIVE_APPLICATIONS_PER_CANDIDATE = 5`; both the server action and the dialog import it. Error message also interpolates the constant. |
| C-010 | Medium | `.env.example` | Missing: `GOOGLE_GEMINI_API_KEY`, `CRON_SECRET`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `LINKEDIN_CLIENT_ID/SECRET`, `ZOOM_CLIENT_ID/SECRET`, `MICROSOFT_CLIENT_ID/SECRET`. | Add all with placeholder values. | **Fixed 2026-05-23** — `.env.example` rewritten with sectioned headers (Supabase / App / Email / OAuth / CV / Turnstile / Sentry); all listed vars added plus `NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL` and `TURNSTILE_SECRET_KEY`. Inline comments explain each. |
| C-011 | Medium | `lib/env.ts` | Does **not** validate `GOOGLE_GEMINI_API_KEY`, `CRON_SECRET`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`. Tracked as `C-env-validation-gaps`. | Add to env schema. | **Fixed 2026-05-23** — all six promoted into `lib/env.ts` (server schema: `CRON_SECRET`, `SENTRY_ORG`, `SENTRY_PROJECT`; client schema: `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `NEXT_PUBLIC_SENTRY_DSN`). All optional — they degrade features gracefully when absent. `GOOGLE_GEMINI_API_KEY` was already added when fixing S-019 earlier in the session. |
| C-012 | Medium | `tsconfig.json:11` | `strict: true` but no `noUncheckedIndexedAccess` / `exactOptionalPropertyTypes`. | Enable both. | **Deferred 2026-05-23 — too large for incremental cleanup.** Enabling `noUncheckedIndexedAccess` produces 87 errors across ~40 files (`__tests__/guides.test.ts`, `lib/__tests__/sentry-scrub.test.ts`, `app/(dashboard)/subscription/page.tsx`, `components/candidates/application-evaluation.tsx`, `components/vacancies/vacancy-application-row.tsx`, and many smaller ones). Some are mechanical (`arr[i]!`) but the evaluation set-state pattern (`{ ...prev, [key]: { ...prev[key], text } }`) needs a real fix that changes runtime behavior. The audit itself recommends "Tighten incrementally" — opening a dedicated PR for this when capacity allows. Same scope as A-007. |
| C-013 | Medium | `eslint.config.mjs:23-30` | No `eslint-plugin-jsx-a11y`. | Add it. | **Fixed 2026-05-23** — installed `eslint-plugin-jsx-a11y@^6.10.2` (devDep) and wired its `configs.recommended.rules` in `eslint.config.mjs`. Spread the defaults then explicitly downgraded most rules to `'warn'` and turned off two false-positive-prone ones (`click-events-have-key-events`, `no-static-element-interactions`) so the existing codebase passes — the team can triage warnings incrementally. Lint is clean (`eslint --quiet`). |
| C-014 | Medium | `components/auth/sign-up-form.tsx:64` | Hardcoded `/auth/callback` path appended to `NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL`. | Either expect full URL or extract constant. | **Fixed 2026-05-23** — `AUTH_CALLBACK_PATH = '/auth/callback'` constant added to `lib/types/constants.ts`; sign-up form imports it for the dev-redirect URL rewrite. |
| C-015 | Medium | `next.config.mjs` CSP | `'unsafe-inline' 'unsafe-eval'` in `script-src`. | Plan a nonce-based hardening pass. | **Fixed 2026-05-23 (Phase 1)** — see S-014. Per-request nonce infrastructure landed in middleware; Phase 2 removes the legacy fallbacks once production observability is clean. |
| C-016 | Low | `components/guide/mdx-components.tsx` | Possible hardcoded external URLs. | Audit. | **Fixed 2026-05-23** — no hardcoded external URLs in the overrides map itself. Did improve the `<a>` override to auto-detect external links (`/^https?:\/\//i`) and add `target="_blank"` + `rel="noopener noreferrer"` to defend against reverse-tab-nabbing. Audit of `content/guides/*.mdx` shows only `https://hrhandle.com` references (same-origin), so no further changes needed. |

---

## ♿ Accessibility Issues

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| AC-001 | High | `components/landing/pricing-section.tsx:42-71` | Billing toggle buttons (Monthly / Annual) lack `aria-label` and `aria-pressed`. | Add ARIA + use radix `ToggleGroup`. | **Fixed 2026-05-23** — wrapped toggle in `role="radiogroup"` with `aria-label="Billing cycle"`, each button gets `role="radio"` + `aria-checked` + `aria-label`. Kept the existing custom styling instead of pulling in radix ToggleGroup (light-touch). |
| AC-002 | High | `components/landing/pricing-section.tsx` | Selected billing state not announced to screen readers. | `aria-pressed="true/false"`. | **Fixed 2026-05-23 (via AC-001)** — `aria-checked` on each `role="radio"` button. Also added `focus-visible:ring` for keyboard users. |
| AC-003 | Medium | `components/candidates/contact-card.tsx:22-33` | Copy button has no live-region announcement on copy. | Add `aria-live="polite"` status. | **Fixed 2026-05-23** — added an `sr-only` `role="status"` `aria-live="polite"` span that announces "Copied to clipboard" when state flips. Button `aria-label` also toggles between "Copy" and "Copied to clipboard". Icons get `aria-hidden`. Also wrapped clipboard write in try/catch (defensive). |
| AC-004 | Medium | `components/candidates/candidate-status-select.tsx:72` | Trigger button label only conveyed visually. | Add `aria-label`. | **Fixed 2026-05-23** — dropdown trigger now `aria-label="Candidate status: {name}. Click to change."`. Decorative icons (loader, chevron) get `aria-hidden`. |
| AC-005 | Medium | `components/dashboard/sidebar.tsx:59-65` | Mobile hamburger toggle is icon-only. | `aria-label="Toggle navigation"`. | **Fixed 2026-05-23** — hamburger button gets dynamic `aria-label` (Open/Close navigation) + `aria-expanded` + `aria-controls="dashboard-sidebar"`. Aside gets matching `id` and `aria-label`. Icons get `aria-hidden`. |
| AC-006 | Medium | `app/auth/login/page.tsx:199-210` | Custom-styled "Remember me" checkbox — verify visible focus ring. | Add `:focus-visible` ring or use radix Checkbox. | **Fixed 2026-05-23** — added `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` classes. |
| AC-007 | Medium | `app/page.tsx:142-155` | Stats section uses bare `<div>`s; no semantic heading. | Use `<dl>/<dt>/<dd>` or `<h3>` + caption. | **Fixed 2026-05-23** — section is now a `<dl aria-label="Key metrics">` with each metric as `<dd>` (value) + `<dt>` (label). Flex order used to keep the visual layout (value above label) while preserving semantic order. |
| AC-008 | Low | `components/landing/pricing-section.tsx:26-36` | Campaign icon with no accessible name. | `aria-label="Promotion"`. | **Fixed 2026-05-23** — `<Zap>` icon now has `aria-label="Promotion"`. |
| AC-009 | Low | `app/page.tsx:76-81,283-287` | Decorative `<Briefcase>` icons lack `aria-hidden`. | Add `aria-hidden="true"`. | **Fixed 2026-05-23** — both decorative Briefcase icons (nav + footer) marked `aria-hidden="true"`. |
| AC-010 | Low | `components/auth/sign-up-form.tsx:16-35` | OAuth icons OK (aria-hidden), but verify button text remains visible. | Spot-check. | **Verified 2026-05-23** — `GoogleIcon` and `MicrosoftIcon` JSX both render `aria-hidden="true"` on their SVGs; the buttons display visible text ("Sign in with Google", "Sign in with Microsoft") alongside. No change needed. |
| AC-011 | Low | `app/(dashboard)/layout.tsx` | No skip-to-content link. | Add `<a href="#main" className="sr-only">…</a>`. | **Fixed 2026-05-23** — `<a href="#dashboard-main">` skip link added at the top of the layout (sr-only by default, visible on focus with focus-ring). `<main>` got `id="dashboard-main"` + `tabIndex={-1}` so the focus moves cleanly. |
| AC-012 | Low | `components/candidates/candidate-status-select.tsx:29-36` | Status colour conveys meaning — verify text + contrast. | Test against WCAG AA. | **Deferred 2026-05-23** — needs real WCAG contrast measurement on every status palette pair (light + dark themes). Add to a dedicated browser-based accessibility audit pass; the text alone already conveys meaning (status name is rendered), so the colour is reinforcement rather than sole signal. |

---

## 🔐 Compliance / Data Protection

| # | Severity | File:Line | Description | Suggested Fix | Status |
|---|----------|-----------|-------------|---------------|--------|
| G-001 | High | `lib/cv-parser.ts` + `app/privacy/page.tsx` §5.1 | Gemini API key is on the **free / unpaid tier**. Google's terms (verified 2026-06-03 at https://ai.google.dev/gemini-api/terms) (a) permit Google to use submitted content "to provide, improve, and develop Google products and services", and (b) explicitly prohibit submission of personal information to Unpaid Services. Two consequences: (1) every CV parsed so far technically violated Google's own terms; (2) Privacy Policy §5.1 currently claims *"We use Google's paid Gemini API, under terms which prohibit Google from using customer prompt content to train their models"* — currently a **false statement in a public policy**. The EEA/UK/Switzerland safe-harbour in Google's terms does not apply because the controller is registered in Georgia (outside the EEA). **Calibration:** low real risk while only 3 friend testers use the parser (no real EU candidate traffic yet); becomes a hard blocker before any public launch or paid customer. | Enable billing on the Google Cloud / AI Studio account associated with `GOOGLE_GEMINI_API_KEY`. Gemini 2.5 Flash paid pricing is ~$0.075 per 1M input tokens — cents per month at realistic ATS volume. After enabling: verify the API still works, save a screenshot of the "Paid Services — Google doesn't use your prompts … to improve our products" section, then keep policy §5.1 unchanged. Alternative: gate CV parsing on an env var until billing is enabled. Do **not** ship publicly without one of these paths complete. | **Open — deferred 2026-06-03** per founder decision to skip and continue with other compliance items. Privacy policy §5.1 currently overstates the protection in place; left unchanged for now to avoid policy churn while the underlying fix is deferred. Re-open before any public marketing or real EU candidate traffic. |
| G-002 | High | `components/apply/apply-form.tsx` | Public apply form only showed a single-line notice (*"By submitting, you agree to your information being stored for recruitment purposes."*) — failed GDPR Article 13 disclosure-at-point-of-collection: did not identify the controller (the recruiting org), did not link to a privacy policy, did not mention the automated CV extraction, did not state retention, and did not surface candidate rights. Also a sales-trust gap — Workable/Recruitee competitors all have proper notices. | Render an Article 13 notice block above the submit button: names the recruiting org as data controller (threaded via new `companyName` prop from the apply page), discloses HRHandle's processor role, lists data collected including the automated CV extraction (worded neutrally given G-001), states retention in line with privacy §7, and routes rights requests to the controller with a link to `/privacy`. | **Fixed 2026-06-03** — new notice block in `components/apply/apply-form.tsx` above the submit button; `app/apply/[token]/page.tsx` passes `companyName={org?.name \|\| 'this company'}` into the form. Old one-line footer removed. No checkbox — legal basis is Art. 6(1)(b) pre-contractual, not consent. |
| G-003 | High | `app/api/cron/expire-vacancies/route.ts` (only) — no purge cron existed | Privacy policy §7 promised *"permanently removed within 30 days"* for soft-deleted candidate data, but `deleted_at` rows in `candidates`, `applications`, `candidate_documents`, `candidate_notes`, `custom_fields`, `vacancies` and the corresponding files in the `candidate-documents` Storage bucket sat in the DB forever. The policy statement was unenforced — a public commitment without code to back it. | Add a daily Vercel Cron that hard-deletes rows past the 30-day threshold (cascade-aware: deleting `candidates` cascades to all candidate-scoped child tables via existing FKs) and removes the corresponding storage files (best-effort). Schedule offset from `expire-vacancies` (03:00 UTC). | **Fixed 2026-06-03** — new `app/api/cron/purge-deleted/route.ts` (8 unit tests) + `vercel.json` cron entry. Cascade-aware row deletes, per-row vacancy purge with try/catch for the `candidate_evaluations.vacancy_id` RESTRICT FK (skipped vacancies logged + counted, don't fail the run), best-effort storage file cleanup. Returns counts JSON for observability; backend.md updated. Storage orphan reconciliation (when storage.remove fails) deferred to a future smaller task — does not affect row-level GDPR compliance. |
| G-004 | Medium | (no doc existed) | No documented breach response procedure. GDPR Art. 33(5) requires that controllers and processors document all personal data breaches — including breaches that don't trigger external notification — and Art. 33 itself gives a 72-hour clock to notify the supervisory authority. Without a written plan, a solo founder hitting an incident at 2am has to figure out the right sequence under stress. | Write a practical playbook covering: HRHandle's controller-vs-processor split (different notification chains), what counts as a breach, severity tiers, the 72-hour clock, an hour-by-hour response timeline (Hour 0–1 / 1–24 / 24–72 / post-72), notification templates for the supervisory authority + customer-controller + affected individuals, an internal breach log format, quarterly review checklist, and a contacts/resources table. Live at `docs/9-compliance/breach-response.md`. | **Fixed 2026-06-03** — created `docs/9-compliance/breach-response.md`. Calibrated for a solo-founder reality: works when one person is the only respondent. Cross-references G-001/G-002/G-003 where they intersect with breach response. Flags TODOs for verifying the current Georgian supervisory-authority submission channel and for filling in the future EU representative details (linked to the EU-rep item in the audit plan). |
| G-005 | Medium | (no doc existed) | No Records of Processing Activities (ROPA) per GDPR Art. 30. HRHandle does **not** qualify for the Art. 30(5) under-250-employees exemption: candidate-data processing is continuous (not occasional), CV content can incidentally include Art. 9 special categories (health, religion, trade-union, etc.), and the AI-assisted CV parsing introduces risk. Without a ROPA, HRHandle cannot answer Art. 30 requests from a supervisory authority and cannot give a structured account of its processing to customer-controllers performing due diligence. | Write a single ROPA document at `docs/9-compliance/ropa.md` with two halves: (1) Art. 30(1) controller activities for account/org/billing/telemetry data — 5 activities (C-1 through C-5); (2) Art. 30(2) processor activities on behalf of each customer for candidate data — 5 activities (P-1 through P-5). Each activity covers purpose, legal basis, data-subject categories, personal-data categories, sub-processors, retention, transfer mechanism, and security reference. Three appendices: sub-processor summary, cross-border transfer mechanisms, Art. 32 security measures index linking back to existing architecture + middleware/CSP/RLS sources rather than duplicating. | **Fixed 2026-06-03** — created `docs/9-compliance/ropa.md`. Reflects the actual product (no aspirational entries); marks G-001 (Gemini free-tier) inline on the P-3 activity; marks LemonSqueezy + EU rep + DPA as planned/not-yet-in-place. Includes a review log so subsequent edits are tracked. |
| G-006 | Low | `app/api/auth/{google,microsoft,zoom}/disconnect/route.ts` | OAuth disconnect routes NULL the stored tokens locally but never call the provider's revoke endpoint, so the tokens remain valid upstream until natural expiry. Hygiene gap rather than a real exposure (the tokens are unreachable from HRHandle's side once NULLed), but the implementation didn't match a reasonable user expectation of "Disconnect" meaning "revoked end-to-end". | For Google and Zoom, call the provider's documented OAuth revoke endpoint as a best-effort step before clearing the local row — failure logs and continues so the user is never blocked. For Microsoft, no upstream call is possible (Entra has no documented programmatic OAuth 2.0 revoke endpoint for refresh tokens — `end_session_endpoint` is a global sign-out, too aggressive for a per-integration disconnect); document the limitation in the route file and the integration doc, and point users to `myaccount.microsoft.com` if they want absolute revocation. | **Fixed 2026-06-03** — `revokeGoogleToken` added in `lib/google/calendar.ts` (POST to `oauth2.googleapis.com/revoke`, treats 200 and 400 as success); `revokeZoomToken` added in `lib/zoom/meetings.ts` (POST to `zoom.us/oauth/revoke` with Basic auth on client creds, same success criterion). Wired into the Google and Zoom disconnect routes ahead of the local NULL; both wrapped in try/catch so a transient revoke failure doesn't block the local disconnect. Microsoft disconnect unchanged in behaviour, but now carries a multi-paragraph code comment explaining the absence and pointing users to the My Account page. 10 unit tests in `lib/__tests__/oauth-revoke.test.ts`. Docs updated in `google.md`, `zoom.md`, `microsoft.md`. |
| G-007 | Medium | (no UI existed) + `organizations` schema | No self-serve "Delete Organization" path. Users wanting to delete their account had to email `hrhandle26@gmail.com`. Two consequences: (1) GDPR Art. 17 erasure is technically supported but slow and offline; (2) Privacy Policy §7's "30-day grace period then permanent deletion" promise had no in-product mechanism — the purge cron from G-003 only swept candidate-scoped tables, not whole orgs. The `organizations` table had no `deleted_at` column at all. | Add `deleted_at` to `organizations` (migration 031). Build a server action `deleteOrganization` (owner-only, requires confirm-by-typing-org-name, sets `deleted_at` + signs out + redirects). Add a `<DangerZone>` card at the bottom of `/settings/organization` with an `AlertDialog` confirm modal. Add a public `/onboarding/account-deletion-scheduled` page showing days-remaining and email-to-cancel instructions. Add a chokepoint in the dashboard layout: if `organization.deleted_at` is set, redirect every member (owner + team) to the scheduled-deletion page. Extend the daily purge cron with a Step 0 that hard-deletes orgs past the 30-day threshold (cascade does the heavy lifting on child tables) and calls `supabase.auth.admin.deleteUser()` for each former member's `auth.users` row (since `profiles.id` is a FK to `auth.users.id`, cascade-deleting the profile leaves the auth user behind without this). | **Fixed 2026-06-03** — full flow shipped. New: `scripts/031_add_organizations_deleted_at.sql` (migration; **must be applied manually to staging and production Supabase via Dashboard → SQL Editor before deploy**), `lib/actions/organization.ts:deleteOrganization` (8 unit tests), `components/settings/danger-zone.tsx`, `app/onboarding/account-deletion-scheduled/page.tsx`, `components/auth/sign-out-link.tsx`. Modified: `app/(dashboard)/layout.tsx` (deleted_at in profile select + chokepoint), `app/(dashboard)/settings/organization/page.tsx` (renders DangerZone), `app/api/cron/purge-deleted/route.ts` (Step 0 org-purge + auth.users cleanup, 2 new tests). Docs updated in `backend.md`. |
| G-012 | Medium | `app/(dashboard)/candidates/[id]/page.tsx` (no AI assist for post-interview notes) | After interviews, recruiters either type loose free-text notes on the candidate or skip note-taking entirely because structuring them is tedious. Inconsistent or absent notes degrade calibration across recruiters and across rounds, and make later hiring decisions harder to defend if challenged. An AI tool to structure raw notes — strictly advisory, never deciding — is a high-leverage feature with the same compliance shape as candidate summary (sends candidate-related data to Gemini). | Build a sidebar tool on the candidate detail page: recruiter pastes raw notes (50-8000 chars), AI returns five structured sections (summary, strengths, concerns, skills demonstrated, follow-ups). Per-section Copy + explicit "Save as note" via existing `createNote` action. Strict prompt rules: never infer protected characteristics, never make a hiring recommendation, never include candidate's salary in output, factual not judgemental, TOO_THIN escape hatch. Per-org rate limit 100/hr. Audit log records feature + raw-notes length only — never the notes content. | **Fixed 2026-06-05** — new `lib/ai/note-extractor.ts` (two-model Gemini fallback, JSON output parsed + validated with array-string filtering, "all empty" → malformed guard, length-based too_thin guard), new `app/api/ai/note-extractor/route.ts` (auth + per-org 100/hr rate limit + candidate fetch scoped to org + role context from most-recent application + `writeAuditLog` with raw_notes_length but never the content), new `components/candidates/ai-notes-extractor.tsx` (collapsible sidebar panel, length validation client-side, per-section Copy + "Save as note" via existing `createNote` action with traceability prefix). Mounted at the top of the right sidebar on the candidate detail page. Privacy §5.1, ROPA P-3, ai-features.md, overview.md all updated. 12 unit tests in `lib/__tests__/ai-note-extractor.test.ts` covering success, model fallback, JSON parsing (with code-fence strip), too_thin (length + sentinel), malformed (non-JSON, missing summary, all-empty), summary-only edge case, no_key, prompt guards (protected-class, hiring recommendation, salary, candidate name + role + verbatim notes, TOO_THIN), and pre-trim length boundary. |
| G-011 | Medium | `app/(dashboard)/vacancies/[id]/page.tsx` (no AI assist for interview prep) | Recruiters preparing interviews drafted questions from scratch each time. The vacancy fields (title, description, responsibilities, requirements) are exactly the context an AI needs to suggest role-specific questions; without an in-product feature this work happened off-platform or not at all, leading to lower-quality interviews and inconsistent calibration across recruiters. | Add an interview-questions feature as a new tab on the vacancy detail page. Follow the G-009 design principles. Generate 4 categories at once (behavioural / technical / situational / closing) with strict prompt guards against protected-characteristic probing and against asking about salary at interview time. Persist saved sets to a new `vacancies.interview_questions` JSONB column (migration 032) — explicit "Save to vacancy" button, never auto-saved. Per-question Copy + per-question Delete on the saved view. Per-org rate limit 100/hr. No candidate data sent to the AI. | **Fixed 2026-06-05** — new `scripts/032_add_vacancies_interview_questions.sql` (migration **must be applied manually via Supabase Dashboard → SQL Editor on both staging and production**), `lib/ai/interview-questions.ts` (two-model Gemini fallback, structured JSON output parsed + validated, strict prompt rules against protected-class probing + salary questions + hype language), `app/api/ai/interview-questions/route.ts` (auth + per-org 100/hr rate limit + `writeAuditLog` with `details.feature = 'interview_questions'`), `lib/actions/interview-questions.ts` (`setInterviewQuestions` server action, Zod-validated, org-scoped, audit-logged), `components/vacancies/ai-interview-questions.tsx` (Saved section with per-question Copy + Delete, AI suggestion panel with optional context, per-question Copy, per-category Copy-all, explicit "Save to vacancy" with confirm-before-overwrite). New 4th tab "Interview questions" on `app/(dashboard)/vacancies/[id]/page.tsx`. 13 unit tests in `lib/__tests__/ai-interview-questions.test.ts`. Privacy §5.1, ROPA P-3, ai-features.md, overview.md updated. |
| G-010 | Medium | `components/vacancies/vacancy-form.tsx` (no AI assist) | After G-009 established the AI-features framework with candidate summary as the first feature, vacancy creation was the next natural target. Drafting a job description is one of the most time-consuming tasks for recruiters and one where AI assistance is well-established as safe (no candidate data involved, output is reviewed before publish). | Add a JD generator following the same six design principles as G-009: button-triggered, advisory output, no auto-fill, audit-logged. Generate per-section (description / responsibilities / requirements) so recruiters can drive each section independently. Provide per-section Copy buttons (clipboard, manual paste) **and** an "Apply all to form" action that explicitly replaces the form fields — confirmed via a native dialog when any field is non-empty. Per-org rate limit 100/hr matching candidate summary. No candidate data sent to the AI for this feature. | **Fixed 2026-06-05** — new `lib/ai/jd-generator.ts` (`generateJobDescriptionSection(input, section)` — Gemini two-model fallback with strict shared rules: no rockstar/ninja/etc, no fabricated companies/tech/perks, role-neutral third person, TOO_THIN escape hatch), new `app/api/ai/jd-generator/route.ts` (auth + per-org 100/hr rate limit + `writeAuditLog` with `details.feature = 'jd_generator'` and the section name), new `components/vacancies/ai-jd-suggest.tsx` (collapsible card with optional "context for AI" textarea, per-section Generate / Regenerate / Copy, and an "Apply all to form" button with confirm-before-overwrite). Mounted at the top of the "Vacancy Details" card in `<VacancyForm>` via `getFormSnapshot` / `getExistingFieldText` / `onApplyAll` props. Privacy §5.1, ROPA P-3, and ai-features.md updated. 12 unit tests in `lib/__tests__/ai-jd-generator.test.ts`. |
| G-009 | Medium | (no AI feature beyond CV parsing existed) | After the post-launch compliance sprint, HRHandle had a single AI feature (CV parsing on the apply form). No structured framework existed for adding further AI features safely (design principles, advisory-only stance, EU AI Act-aligned logging, prompt-update policy). Without these, each new AI feature risks drifting into Article 22 territory or accumulating untraceable model calls. | Establish the AI-features framework with the candidate-summary feature as the first concrete implementation: 6 documented design principles (assistant-only, honest labelling, minimum-necessary data, traceability via `writeAuditLog`, graceful degradation, human-readable failures), explicit `ai_assist` audit log action, candidate-summary endpoint + UI panel + Gemini integration following the cv-parser pattern (two-model fallback, timeout, fail-soft return type), privacy + ROPA updates, and a new policy doc `docs/9-compliance/ai-features.md` covering EU AI Act mapping, prompt-update policy, and quarterly review. Bundle a parse-cv rate-limit bump (10→30 per IP/hour) since the original 10/hr was too tight for legitimate applicant traffic. | **Fixed 2026-06-05** — new `lib/ai/candidate-summary.ts` (minimum-necessary input shape, two-model Gemini fallback, "too thin" guard against fabrication), new `app/api/ai/candidate-summary/route.ts` (auth + per-org rate limit 100/hr + audit log + delegate), new `components/candidates/ai-summary-panel.tsx` (button-triggered, prominently labelled, "Save as note" via existing notes action), mounted at top of left column on the candidate detail page. New policy doc `docs/9-compliance/ai-features.md`. Privacy §5.1 rewritten to cover both AI features and clarify Article 22 stance; ROPA P-3 renamed and expanded. `parse-cv` rate limit bumped to 30/hr/IP in same PR. Tests: `lib/__tests__/ai-candidate-summary.test.ts`. G-001 references retained inline — paid-tier remains a prerequisite before real EU candidate traffic. |
| G-008 | Medium | (no gate existed) | No sign-up country gate. New accounts could be created from any jurisdiction, including FATF call-for-action and OFAC/EU/UK comprehensively sanctioned countries. With the planned switch from LemonSqueezy (Merchant of Record — handles sanctions screening on their side) to Flitt (payment gateway — HRHandle is the seller of record), the sanctions-screening burden shifts to HRHandle. A documented good-faith country gate is now needed before public launch / paying customers. | Implement an IP-country gate at new account creation only. Block list = union of FATF call-for-action + OFAC/EU/UK comprehensive country sanctions: `KP, IR, MM, SY, CU, BY, RU, VE`. Source the country code from Vercel's `x-vercel-ip-country` header (server-side, no client tampering). Gate fails open when the header is absent (local dev, tests, Vercel-can't-determine cases) — false-positive lockouts are worse than the marginal extra exposure. **Apply to** `/auth/sign-up` (server-component render) **and** the dashboard layout's "no organization yet" branch (closes the OAuth loophole where a user from a blocked country clicks "Continue with Google" on `/auth/login` and goes straight to onboarding). **Do not apply to** `/auth/login` (existing customers may travel), `/apply/[token]` (candidates are data subjects, not counterparties — gating creates a hiring-discrimination problem), or `/jobs/[slug]` (public read-only). Document the policy, quarterly review cadence, and authoritative source pointers. | **Fixed 2026-06-04** — new `lib/sanctions.ts` (block list constant + `getRequestCountry` / `isBlockedCountry` / `getBlockedCountry` helpers, 14 unit tests in `lib/__tests__/sanctions.test.ts`); new `app/not-available/page.tsx` (friendly "not available in your region" page, robots-noindex, links to email contact + privacy/terms); modified `app/auth/sign-up/page.tsx` (server-side header read + redirect before form render); modified `app/(dashboard)/layout.tsx` to close the OAuth loophole in the "no organization yet" branch. New policy doc `docs/9-compliance/sanctions-screening.md` with quarterly review checklist and changelog. VPN bypass acknowledged in code comments and the doc. Entity / individual SDN screening explicitly out of scope here; tracked for future when paying-customer revenue justifies a managed screening service (Sumsub / ComplyCube / OpenSanctions). |

---

## Notes on Methodology

- 10 parallel research agents inspected the codebase from product, business, architecture, integrations, API, UI text, security, bug, quality, and config/a11y angles.
- Issues are deduplicated and re-graded for severity.
- Verification of live Supabase schema state was **not** performed for this pass — the keys in `.env.local` are the new Supabase publishable/secret format and return 401 against the REST API from the local environment. All DB facts in this audit are taken from `supabase/migrations/`.
- "Status" is `Open` for every issue. Update inline when fixed (`Open → In Progress → Fixed`).
