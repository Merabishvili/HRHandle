---
title: Manage candidates
summary: Add, edit, and organize candidates in your talent pool.
updated: 2026-05-21
---

The Candidates section is your central talent pool. Every applicant — whether added by hand, parsed from a CV, or submitted through the public apply form — appears here.

## Open the candidates list

From the sidebar, click **Candidates**. You will see the list of every candidate in your organization, with search, filters, and an **Add Candidate** action.

<Screenshot
  src="/guide/screenshots/manage-candidates-list.png"
  alt="The Candidates list with the Add Candidate button highlighted"
  caption="Click Add Candidate to create a new record."
/>

## Choose how to add the candidate

HRHandle offers two paths so you don't waste time on data entry when the CV already has everything.

<Screenshot
  src="/guide/screenshots/manage-candidates-entry-mode.png"
  alt="The Add Candidate page showing two options: Upload CV first or Fill manually"
  caption="Upload CV first to auto-fill the form; Fill manually if there is no CV yet."
/>

- **Upload CV first** — Upload a PDF or Word file. HRHandle parses it with AI and pre-fills name, email, phone, LinkedIn URL, work history, and education. Review and submit.
- **Fill manually** — Open the empty form and type details by hand. You can still attach a CV as a document later.

Either path saves to the same candidate record.

## View and edit a candidate

Click any row in the list to open the candidate detail page. From there you can:

- Edit personal details and contact information
- Add or remove work experience and education entries
- Attach documents (CVs, cover letters, portfolios)
- Add internal notes
- Change the candidate's general status (Active, Hired, Archived)
- See every application that links them to a vacancy

<Screenshot
  src="/guide/screenshots/manage-candidates-detail.png"
  alt="The candidate detail page for Lukas Becker showing contact info, experience, and actions"
  caption="Each section can be edited inline. The right column lists vacancies the candidate is linked to."
/>

## Search and filter the list

The top of the candidates list has a search box and status tabs.

- **Search** matches against first name, last name, and email.
- **Status tabs** filter by In progress (მიმდინარე), Inactive (არააქტიური), or Hired.
- **Columns** lets you choose which fields appear in the table — useful if you store custom data like preferred languages or notice period.

## General status vs. application status

These are two different things:

| Status type | Where it lives | What it means |
|---|---|---|
| **General status** | On the candidate | Their relationship with your organization. **Auto-derived**, not manually set: **In progress** (მიმდინარე) = has at least one open application; **Inactive** (არააქტიური) = no open application (never added to a vacancy, or all applications closed); **Hired** (დაქირავებული) = hired. |
| **Application status** | On each application (candidate ↔ vacancy) | Where they are in a specific hiring pipeline — Applied, Screening, Interview, Offer, Hired, Rejected. |

The general status follows the applications automatically: adding a candidate to a vacancy makes them **In progress**; when all their applications close (rejected/withdrawn/hired), they fall back to **Inactive**; moving an application to **Hired** syncs the candidate to **Hired**. Bulk-imported and manually-added candidates start **Inactive** until you add them to a vacancy.

## Export and bulk actions

The list supports CSV export of every candidate visible after filters and search — useful for offline reporting or compliance. Click the **Export CSV** button next to **Add Candidate**.

## Bulk import from CSV

Owners and admins can bulk-import candidates from a spreadsheet via **Bulk import** on the candidates page. The flow is **upload → review → confirm → import** on a single page (no column-mapping step):

1. **Upload.** Download the **Excel (.xlsx) template** and fill it in — it opens as proper columns in Excel and Numbers, with none of the comma/encoding traps of hand-edited CSVs. You can upload **.xlsx or .csv**; the file must use the template's exact header row (columns matched case-insensitively; unknown or missing-required columns are rejected at upload). Only **first name** and **last name** are required — **email is optional**. Limits: 5,000 rows, 10 MB. CSVs are decoded by their real encoding (UTF-8 or UTF-16, including files re-saved by Excel/Numbers), and comma and semicolon delimiters are both accepted.
2. **Review.** The file is parsed server-side and every row is validated. Errored rows sort to the top; each bad cell is highlighted with a reason (invalid email, phone, experience, LinkedIn, salary, or a **duplicate** email — already in your org, or repeated in the file). You can **edit any cell inline** or **delete rows**; the ready/error counters update live. You cannot proceed while any error remains — fix it or delete the row. Your edits are saved automatically and survive a page refresh for 24 hours.
3. **Confirm & import.** A read-only preview of exactly what will be created, then the import runs as a background job with a live progress bar (it keeps running if you close the tab; you can cancel between batches — already-created candidates remain). When it finishes you get a count of imported candidates and a link to view just this import's candidates.

Duplicates are never merged and existing candidates are never updated — a duplicate is an error you resolve by editing the email or deleting the row. Imported candidates count against your plan's candidate limit. Every imported candidate is stamped with the import so you can filter the list to a single import (`?import=<id>`).
